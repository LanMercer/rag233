# 对接说明（A 线 · 博客发布施工图）

> **给谁看**：接手"把这篇文章发布到博客"的**另一个 agent / 另一位执行者**（同机操作）。
> **本项目不操作博客仓库**——本文件是唯一的施工依据；本目录（`第四周\发布包\`）是唯一素材出口。
> **写作日期**：2026-09-22（晚）。**下列 8 条均已实地核对过**（不是推测）。

---

## 一、任务一句话

把 `第四周\发布包\blog\项目展示页.md` + `blog\assets\` 下的两张图，**发布到 Hexo 博客**（仓库 `LanMercer/LanMercer.github.io`），
拿到**可公开访问的 URL**，并**回填到 3 个文件**（见第六节）。

**不要改文章里的任何数字或结论**。数字的唯一来源是本项目内的 `第四周\day20\定稿数字表.md`
（由脚本从 `run_log.txt` 机器抄录 + 自动校验 F1↔混淆矩阵自洽）；要改数字，**先在项目里改，再重新拷过去**。

---

## 二、素材清单（本项目提供的东西）

| 文件 | 说明 | 体积 |
|---|---|---|
| `blog\项目展示页.md` | 展示文章**定稿**（数字已回填、三个 URL 已填实、含"原模型优于微调"的负结果） | < 50 KB |
| `blog\assets\项目架构图.png` | 文章第 1 张图（四层架构） | 211.1 KB |
| `blog\assets\优化前后对比.png` | 文章第 2 张图（before/after 柱状图），**同时用作 `cover:`** | 187.4 KB |
| `blog\assets\demo演示.gif` | ⚠ **尚未提供**（录屏后另给）。**没有就先用 2~3 张截图代替**，或在文章里暂时移除该引用 | —— |
| `space_demo\` | B 线（在线 Demo）源码，**与博客无关**，仅供了解上下文 | —— |

---

## 三、8 件必须知道的事（⭐ 已实测核对，照做即可）

| # | 事项 | 实测结论 |
|---|---|---|
| 1 | **目标仓库** | `https://github.com/LanMercer/LanMercer.github.io.git`；本地工作副本 **`D:\trae.wangye\hexo-blog-v2`**（Hexo **7.3.0** + butterfly 主题） |
| 2 | **仓库结构机制** ⭐ | **仓库根只存 `_hexo\public\` 的生成产物**（判据：根 `index.html` 与 `_hexo\public\index.html` 的 MD5 **完全相同** = `C28B4C356D868EF717332B69FCD7A53B`）；`.gitignore` **只有一行** `_hexo/` |
| 3 | **文件落位** | 文章 → `_hexo\source\_posts\<slug>.md`；图片 → `_hexo\source\images\<slug短名>\` |
| 4 | **图片路径约定** ⭐ | 文章里必须写**站根绝对路径** `/images/<slug短名>/xx.png`。现有惯例：文章 `llm-rag-project-ai-workflow` → 图片目录 `llm-rag-project`。⚠ **现稿写的是 `./assets/xx.png`（共 3 处：`cover:` 1 处 + 正文 `![]()` 2 处）**，不改就是**裂图** |
| 5 | **front-matter 要点** | `date:` **必须带时分秒**（现稿只有日期）→ 例 `2026-09-22 10:00:00`；补 `categories: 实践总结`；`cover:` 改成站根路径；**建议补 `description`**；`tags` 现稿已有（`大模型, RAG, QLoRA, 项目复盘`） |
| 6 | **构建与同步命令** | `npx hexo clean && npx hexo g` → 校验 `_hexo\public\2026\09\22\<slug>\index.html` **确实生成** → 再把 `_hexo\public\*` **拷到仓库根** |
| 7 | **push 必须挂代理** ⭐ | 实测：**直连 `github.com:443` 不通**，`127.0.0.1:7897` 代理通（HTTP 200）。用：`git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 -c http.sslBackend=openssl -c http.version=HTTP/1.1 push`（三层坑是**串联**的，报新错就按顺序逐层排） |
| 8 | **四条禁令** ⚠ | ① **禁用 `robocopy /MIR` 或 `/PURGE`**——会把不在 `public\` 里的 `_hexo\`、`.gitignore`、`README.md` 一起删掉（`_hexo\` 一删，Hexo 源码就没了）；② **不要动** `website-preview-20260904\`、`LanMercer.GitHub.io\`、`hexo-blog-v2-backup\`（避免"改了两处只推一处"）；③ **不要改文章里的数字**；④ **不要在博客仓库里新增/删除 LFS 文件** |

### 3.1 URL 规则（可以先把 URL 推定出来）

- 站点 `url: https://lanmercer.github.io`，`permalink: :year/:month/:day/:title/`
- 已有先例：09/13 那篇 = `https://lanmercer.github.io/2026/09/13/llm-rag-project-ai-workflow/`
- 因此本篇（`date` = 2026-09-22）预期的 URL 形如：`https://lanmercer.github.io/2026/09/22/<slug>/`
- ⭐ **建议给文章一个英文 slug**（避免中文标题被 URL 编码）：在 front-matter 加
  `permalink: /2026/09/22/rag-doc-qa-optimization/`（或把文件名定为 `rag-doc-qa-optimization.md`）。
  **以你 `npx hexo g` 后实际生成的目录名为准**，别凭空写。

---

## 四、建议执行步骤（命令级）

```powershell
# ①② 准备与落位（路径按你的实际仓库改）
$BLOG = "D:\trae.wangye\hexo-blog-v2"
$PKG  = "D:\Lan\研究生\技术学习\大模型算法\第四周\发布包"

# 图片：放到 _hexo\source\images\<slug短名>\
New-Item -ItemType Directory -Force "$BLOG\_hexo\source\images\rag-doc-qa" | Out-Null
Copy-Item "$PKG\blog\assets\*.png" "$BLOG\_hexo\source\images\rag-doc-qa\" -Force

# 文章：拷进 _hexo\source\_posts\  （文件名即 slug，按第三节 3.1 的建议取名）
Copy-Item "$PKG\blog\项目展示页.md" "$BLOG\_hexo\source\_posts\rag-doc-qa-optimization.md" -Force

# ③ 改 3 处图片路径 + front-matter（这是本节唯一需要"动手改"的地方）
#    - cover: ./assets/优化前后对比.png        →  /images/rag-doc-qa/优化前后对比.png
#    - ![](./assets/项目架构图.png)            →  /images/rag-doc-qa/项目架构图.png
#    - ![](./assets/优化前后对比.png)          →  /images/rag-doc-qa/优化前后对比.png
#    - date: 2026-09-22                       →  2026-09-22 10:00:00
#    - 补 categories: 实践总结 / description / permalink（见 3.1）

# ④ 构建 + 校验（关键：确认目标 HTML 真的生成了）
cd $BLOG\_hexo
npx hexo clean; npx hexo g
Get-Item ".\public\2026\09\22\rag-doc-qa-optimization\index.html"   # ← 必须存在

# ⑤ 本地预览（可选但强烈建议：先看裂图与表格）
npx hexo s      # 浏览器 http://localhost:4000

# ⑥ 同步到仓库根（⚠ 不要用 robocopy /MIR）
Copy-Item ".\public\*" $BLOG\ -Recurse -Force

# ⑦ push（记得挂代理，见下表第 7 条）
cd $BLOG
git status -sb
git add -A
git commit -m "新增：机器人领域RAG文档问答系统项目展示页（含优化前后对比）"
git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 `
    -c http.sslBackend=openssl -c http.version=HTTP/1.1 push
```

---

## 五、发布后的验收（逐条打勾）

- [ ] `_hexo\public\2026\09\22\<slug>\index.html` **生成成功**
- [ ] 线上 URL 可打开：`https://lanmercer.github.io/2026/09/22/<slug>/`
- [ ] **两张图都显示**（架构图 + 优化前后对比图，**不是裂图**）
- [ ] Markdown 表格**排版正常**（表头有竖线，不是一团文字）
- [ ] 那句"**⚠️ 诚实标注**"在页面上**可见**（没被折叠/注释掉）
- [ ] **手机流量（关 WiFi）能打开**，**不需要本机开着**（本机模型服务也**不需要**开着）
- [ ] `git status -sb` 没有 `[ahead N]`
- [ ] **A 线 URL 已回填到下面 3 处**（第六节）

---

## 六、回填清单（拿到 URL 后，请改这 3 处）

| # | 文件 | 填什么 |
|---|---|---|
| 1 | `第四周\发布包\发布包说明.md` → §五 对外链接清单 → `A 线 · GitHub 博客展示页` 一行 | 把 `__` 换成线上 URL 与发布日期 |
| 2 | `第四周\day20\week4工作汇报.md` → §三 交付物 → 第一行 | 把"待回填"换成线上 URL |
| 3 | `第四周\day20\P线-叙事与复述.md` → 简历项目段（`<A 线博客链接>`） | 换成线上 URL（或删除该占位） |

> 完成后请**回复一句话**：`A 线已上线：<URL>`——本项目侧会据此把闸门与看板收尾。

---

## 七、边界与免责（交接时必须一并告知）

1. **在线 Demo 的生成层是 DeepSeek API，不是本项目的微调模型**——文章里、页面上、Demo 的 `README.md` 里**三处**都有这句标注，
   **不要删、不要改**（这是"诚实性"底线）。
2. **文章里的评测数字只归属本地 Qwen2.5-3B-Instruct-LoRA 版本**（不是那个在线 Demo），**不要把它挂到 Demo 上**。
3. 文章里包含**逆预期负结果**（原模型 F1 0.947 > 微调 0.824；数量对齐后仍退化）——**这是刻意保留的，不是笔误**。
4. 本项目与博客仓库**各自独立**：本项目的 `第四周\发布包\` 是素材源，博客仓库只接收**复制过来的副本**。
5. ⚠ 本项目**尚未 git 提交** Day20 产物（截至本文件写作时）；如需引用"仓库里的文件"，注意**先同步到 `rag233`**。

# HuggingFace Spaces 在线 Demo 依赖（CPU basic 免费层）
# 说明：检索层（bge + Chroma）在 CPU 上运行；生成层调用 DeepSeek API（OpenAI 兼容），故不需要 GPU/大模型权重。

gradio==4.44.0

# ⚠️ 必须 pin pydantic 的上界：gradio 4.44.0 只写 pydantic>=2.0（无上界），
# 而 pydantic>=2.11 把 dict 字段的 JSON Schema 从 additionalProperties:{} 改成 true（布尔），
# gradio_client 1.3.0 的 get_type() 里 `if "const" in schema` 拿布尔做 in → TypeError，
# 表现为【服务能起，但首页与每个请求都 500】。
# 本地实测踩过（2026-09-20），HF Spaces 全新容器会装最新 pydantic → 不加这行线上必崩。
pydantic==2.10.6

# RAG 检索链路（与本地项目保持一致：bge-small-zh 向量化 + Chroma 余弦检索）
langchain==0.3.7
langchain-community==0.3.5
langchain-chroma==0.1.4
langchain-huggingface==0.1.2
langchain-text-splitters==0.3.2
chromadb==0.5.15
sentence-transformers==3.2.1

# PDF 读取 + HTTP 调用云端生成接口
pypdf==5.1.0
requests==2.32.3

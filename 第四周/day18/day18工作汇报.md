# Day 18 工作汇报 · 第四周第二次"受控实验"：把瓶颈从"检索参数"钉到"查询侧语言错位"（含一条 RRF 负结果 + 一次**证据链数据错误的自查与修正** + R3 确认跑闭环 + 一次**共享环境被污染后的完整恢复**）

> 汇报人：本人 ｜ 汇报周期：第四周第 3 个工作日（计划日 2026/9/22 周二，**实际执行于 9/19 晚 ~ 9/20**）
> 本日定位：**Day17 的决策门已经定完——"瓶颈不在取几条、也不在编码约定"。今天不再调检索参数，而是用"便宜实验"把这个判断做成可复现的证据。** 三件事：① 先查卷子（O1-R8 数据自检）→ 排除"标注错"这条岔路；② 再做廉价筛查（O1-R4 HyDE 冒烟 + O1-R5 混合检索）→ 把瓶颈钉死在**查询侧**；③ 转 F 线（F1 论文真实 QA 90 条，机器可验证）与 D 线（Web UI 完成版）。**并在核对数字时查出 day17 报告里一处 F1 写错（把"精确率"当成了 F1），已完成修正与全链路同步。**
> **收尾阶段又补了两件事**（本报告末次更新时并入）：④ **R3 确认跑**（`--top-k 8 --runs 3`）完成 → day17 的"推测"变成"实测"，k=8 无前缀的**完整成绩行**正式入表；⑤ **一次自伤式环境事故的完整复盘**——把"给 HF Spaces 用的 `requirements.txt`"装进了共享的 `llm` 环境，**41 个包被降级**、`app.py` 起不来，已定位根因（gradio 4.44 × pydantic ≥2.11 的 Schema 变更）并**按卸载日志逐包恢复**，同时把这个坑**固化成 `requirements.txt` 的 pin 与教程的警告**。
> 依据：`第四周\01-第四周详细计划.md`（**D3 章节**）、`第四周\01-第四周项目总结与优化路线图.md`（O 线 O1-R4/R5/R8 + F 线 F1 + D 线 D2）、`第四周\day17\day17工作汇报.md`（决策门与三条待办）、`第四周\实验日志.md`、当日实际运行产物（`day18\` 全部脚本与结果、`day17\result_lora_k8_runs3\`）与 git 提交记录（`2660b33` / `6aa8cd7` / `27bd6e2` / `01f2d97` / `e703713` / `6ac1de9`）

---

## 一、总背景

**2026 秋招冲刺**：985/211 数学（概率统计）硕士，目标是江浙沪央国企数字化/AI 岗 + 互联网备选。

- **第四周的由来**：第三周已把项目做到"完成"（能演示、能量化、有文档、已存档）。但 HR 看过简历后给了三条意见，最关键的一条是：
  > ①"**项目没有完成，更像是一个流程。最好有一个成果，比如可以来使用；或者有一个优化成果，比如通过什么优化步骤提升了 F1 或召回率。**"
  另外两条（本日直接相关）：
  > ②"**要有可交付的产物（能来使用）**" → D 线：Web UI 完成版 + A/B 双通道在线发布；
  > ③"**300 条机器人行业 SFT 数据如果是随机生成的比较没有意义，需要一些和论文相关的或者真实的数据**" → F 线：**今天要出真实数据的实体**。
- **Day17 打的地基（今天直接用）**：① **校准后的可引用基线 `B0r3`**（k=4 / runs=3 / patch=on，总 40%、一致率 0.950）；② **三组受控对照**（`B0r3` 40% → `R1` 35% → `R1R2` 60%），**每个变量贡献都量化了**（前缀 −1 题、k +5 题）；③ 一条反直觉硬发现——**bge 官方 instruction 前缀在本项目是负贡献**（strict 20% → 0%，机制是"排名位移 1 名撞上 k=4 窗口边界"）→ **前缀回退，`--query-instruction` 默认 off**；④ `start.py` / `start.bat` 一键启动雏形。
- **Day17 决策门定下的 Day18 动作（就是今天的正题）**：
  1. **先关前缀**，默认检索配置 = **k=8 + 无前缀 + patch=on + runs=3**；
  2. **走"几乎没动"侧**：**O1-R8 数据自检**（逐题核对 `source_chunk` 是否真对应答案段落）+ **O1-R4 HyDE 冒烟**（给失败题写"理想查询"）；
  3. **加分项 O1-R5 混合检索**（BM25 + 向量 + RRF）——day17 的失败题**全是专名**（LAFAN1 / BeyondMimic / Stanford / 代码仓库名），稀疏通道正对症；
  4. **另一条独立靶子**：Q14/Q18 在三组里**始终是 FN（编造）**，属**生成层**问题，留给 Day19 模板 09 v2。
- **今日的思维转变（本日真正的收获）**：Day17 之前都在问"**取几条 / 加不加前缀**"；今天换成先问"**卷子对不对 / 错在库还是错在问**"。**先用 30 秒的自检排除"标注错"，再用 35 秒的筛查把"库侧 vs 查询侧"分开——这比再跑一轮几十分钟的全量评测便宜得多，也更容易下结论。**
- **硬件基础**：NVIDIA RTX 3060 Laptop（6GB 显存）；生成链路 Qwen2.5-3B-Instruct-LoRA（`第三周\day13\local_api_lora.py`，4bit + Day12 adapter，实测约 **1.98GB** 显存）。

---

## 二、本周计划与 Day 18 当日计划

**周主题（v2）**：项目整改——**优化成果（O）+ 可交付产物（D）+ 真实数据（F）+ 叙事简历（P）**，其中 D 线含 **A/B 双通道在线发布**（A 线 GitHub 博客静态展示页；B 线 HuggingFace Spaces 在线 Demo，检索层 CPU 真跑、**生成层用 DeepSeek API**）。

**Day 18 当日计划**（对应 `01-第四周详细计划.md` **D3 章节**）：

1. **Day17 决策门落地**：固化默认配置（关前缀 + k=8）；**顺手统一实验日志的日期口径**（day17 待办①，教程列为"开工前先做"）；
2. **O1-R8 数据自检** ⭐：写 `data_selfcheck.py`，逐题核对 `source_chunk` 与期望关键词、**把"检索没召回"区分为真失败 / 标注错位 / chunk 缺失 / 翻译题**；
3. **O1-R4 HyDE 冒烟 + O1-R5 混合检索** ⭐⭐：写 `retrieval_lab.py`，**把检索层单独拎出来**（不调模型、秒级）跑"三通道 × 两种查询模式"，给瓶颈定论；
4. **F1 论文真实 QA** ⭐⭐：从 GMR 论文抽 **≥50 条**真实 QA（带 `source.page/chunk_id/quote`）+ 写 `build_paper_qa.py` 做**六查**（含**数据泄漏检测**）；
5. **D2 Web UI 完成版**：上传 PDF → 建库 → 提问 → 指标对比（从 day16 骨架升级）；
6. **发布包骨架本地跑通**（不上线）；
7. **零散时间**：力扣 **198. 打家劫舍**（一维 DP"选/不选"）＋ 统计八股 6 节串线① ＋ 牛客银行/央企真题 0.5 套；
8. **睡前**：`git check-ignore` 复核 + commit + 写明日计划。

> **收尾阶段追加两件（不在最初计划里，但都是"该做没做"的欠账）**：
> 9. **R3 确认跑**（`eval_v2.py --top-k 8 --runs 3`）——day17 决策门留下的"推测"要变成实测，也是**对外引用 k=8 配置的正式一行**（计划里的第 1 条只做到"固化配置"，没做到"给出完整成绩行"）；
> 10. **`start.py` 指向 `day18\app.py`**（教程 5.4 的一行欠账）+ **`requirements.txt` 补 `pydantic` 上界**（这条是**事故之后才补上的防线**）。

---

## 三、Day 18 工作成果

### 3.1 主线任务（5~7h）

| 任务 | 完成情况 | 说明 |
|---|---|---|
| **`data_selfcheck.py`（O1-R8）** ⭐ 核心产出 | ✅ | 约 340 行。**三重核对**：① `source_chunk` 的 chunk 是否存在；② 期望关键词能否**在标注段落内**字面命中；③ 关键词是否**只出现在别处**（→ 标注错位嫌疑）。另设 `--dump-chunk`（打原文人工核对）、`--scan`（关键字全库扫描）、`SEMANTIC_HINT`（识别"中文概括型关键词"，属翻译题而非标注错）三个机制 |
| **数据自检实跑 + 人工核对** ⭐ | ✅ | 10 条 `in_material` 题：**9 条关键词在标注段落内字面命中；0 条标注错位；0 条 chunk 缺失；1 条（Q8）需人工**。用 `--dump-chunk 3` / `--dump-chunk 0 5` 打原文核对 → **Q8「优于开源 / 接近闭源」确实在 chunk 3、Q10「bone length / joint range of motion / mass distribution」确实在 chunk 5**，两条标注**均正确**，只是关键词用了**中文概括**所以程序查不到 → **❓ 全部消解** |
| **`retrieval_lab.py`（O1-R4 + O1-R5 共用工具）** ⭐ 核心产出 | ✅ | 约 430 行。**把检索层从评测链路里单独拎出来**（不调 LLM）：① `vector`（Chroma + bge）；② `bm25`（**自带实现**，`k1=1.5 / b=0.75`，只吃 ASCII token）；③ `hybrid`（RRF 融合，`rrf_k=60`）；④ `--query-mode plain/hyde`（hyde 用人工写的英文理想查询，当**上界**用）。**判据与 `eval_v2.py` 的双轨完全一致**（strict = Top-K ∩ `source_chunk`；loose = 关键词是否落在 Top-K 上下文），一轮约 **35 秒** |
| **O1-R4 HyDE 冒烟（瓶颈裁决）** ⭐⭐ 今天最重要的一条 | ✅ | **同一套库、同一套判据，只把查询从中文字面换成英文术语 → strict 40% → 100%（三通道一致）**。→ **假设②（中文问句 ↔ 英文段落的语义错位）成立，且量级很大**（详见 3.2） |
| **O1-R5 混合检索（RRF）** ⭐ 负结果，照实记录 | ✅ | plain 模式下 **hybrid 3/10 < vector 4/10**（融合反而**丢了一题**）→ 追出**三个机制**（BM25 对中文零信号却被等权融合 / `1/(60+rank)` 压缩名次差 / 候选池质量不对等），已入「三、失败尝试记录」 |
| **`build_paper_qa.py`（F1 校验器）** ⭐ 核心产出 | ✅ | 约 460 行。**六查**：① 条数门槛；② 必填字段；③ 空字段/重复 instruction；④ **source 可追溯**（`chunk_id` 存在 + `quote` **逐字回原文**比对）；⑤ **数据泄漏**（`difflib.SequenceMatcher` 与 `eval_questions.json` 逐题比，阈值 0.7）；⑥ 五类分布。`chunk_id` 支持**列表**（跨段引用）、`norm()` 归一化（去空白/标点/大小写）、`--allow-leak` 等开关 |
| **`sft_data_paper.json`：论文真实 QA 90 条** ⭐⭐ 核心产出 | ✅ | **实际 90 条**（计划 ≥50）。**逐字回查 90/90 通过、数据泄漏 0 条**。五类分布：**术语定义 18 / 方法步骤 22 / 实验设置 17 / 结论 25 / 局限 8**。每条含 `instruction` / `output` / `source{page, chunk_id, quote}` / `category` |
| **D2 Web UI 完成版 `app.py`** ⭐ 核心产出 | ✅ | 约 470 行，从 day16 骨架升级 **5 件事**：① **上传 PDF → 动态建库**（内存库，不落盘、不污染 day13 的 GMR 库）；② **指标对比表**（**从 `实验日志.md` 现读**，不硬编码）；③ **召回片段可视化**（带相似度分）；④ **Top-K 可调**（现场演示 day17 结论）；⑤ **双模型切换 + 服务身份检查** |
| **app.py 关键 bug 修复** ⭐ | ✅ | **症状**："不上传任何资料、只想看默认知识库"时界面报 `store is None`。**根因**：`_on_build()` 建完库**没把 store 回传给** `store_state`，且 `ask()` 一看到 `store is None` 就直接报错。**修法**：① `_on_build` 返回 `store`；② `build_btn.click` 把返回值接到 `store_state`；③ 新增 `resolve_store()`——`store=None` 时**自动回退到默认 GMR 库**。→ **界面变成真正的"开箱即用"** |
| **`实验日志.md` 记账** ⭐ | ✅ | ① 新增**「9/20（Day18）实测结论」**小节（三通道 × 两模式对照表 + 三条结论 + 默认配置与方向）；② 「三、失败尝试记录」补 **RRF 负结果 1 条**；③ **新增一处数据更正说明**（见 3.2 与 3.5，F1 写错的全过程与影响面） |
| **⚠ 证据链自查：查出并修正 day17 报告的一处 F1 数据错误** ⭐⭐ 本日最该被记住的一件事 | ✅ | 核对数字时发现：`实验日志.md` 与 `day17工作汇报.md` 里 R1R2 的 **F1 写作 0.889，但 0.889 是「精确率 P」，真值是 0.842**。已修正 **4 个文档**（日志 / day17 汇报 / day17 教程 / day18 教程）+ 清理日志中一行重复的占位行；`app.py` 的指标表**读日志、自动跟随**（实测已显示 `F1 0.706 → 0.842 (+0.136)`） |
| **R3 确认跑（`eval_v2.py --top-k 8 --runs 3`）** ⭐⭐ 收尾阶段的"闭环" | ✅ | **day17 的推测 → 实测**：strict **4/10**、loose 5/10、生成 4/10、防幻觉 7/10、**F1 0.824**、总 **11/20 = 55%**、一致率 0.950，已 `--append-log` 入表。**回归的正是 Q3**（前缀曾把它的期望段 chunk 6 挤出前 8；关掉后回到第 6 名）→ **"前缀的伤害是排名位移"这一机制判断成立**。**顺带白捡一条复现证据**：`day16\result_lora_k8`（同配置 runs=1）用同一脚本重算 = **六项汇总逐项相同、逐题 Top-8 与 20 条判定全部一致** → **day16 R2 的 4/10 不是抽样的运气** |
| **⚠ 自伤式环境事故与恢复** ⭐ 本日代价最大、教训最值钱的一次 | ✅ | **事故**：在共享的 `llm` 环境里跑了 `发布包\space_demo\requirements.txt` → **41 个包被降级**（gradio 6.27→4.44、langchain 1.3.14→0.3.7、chromadb 1.5.9→0.5.15、transformers 5.14.1→4.57.6、numpy 2.4.4→1.26.4 …）→ `day18\app.py` **起不来**。**定位**：`launch(theme=...)` 报 `unexpected keyword argument 'theme'`（4.44 的 `launch()` 没有该参数）；更隐蔽的是 **`TypeError: argument of type 'bool' is not iterable` 让"服务能起、每个请求都 500"**——根因是 **pydantic ≥2.11 把 `dict` 的 JSON Schema 从 `additionalProperties: {}` 改成 `true`**，而 `gradio_client 1.3.0` 的 `get_type()` 写的是 `if "const" in schema:`（拿布尔做 `in`）→ 首页与 `/api/info` 全会走它。**⚠ 为什么 gradio 自己没拦住**：`gradio 4.44.0` 只写 `pydantic>=2.0`（**无上界**），pip 认为满足、**连冲突警告都不给**。**恢复**：先配国内镜像（pypi.org 当时 `SSLEOFError`），再**按 pip 卸载日志里的旧版本号逐包装回**；**复验**：`get_api_info()` 能返回数字 + `python app.py` 浏览器点开首页 ✅ |
| **把事故固化成防线** ⭐ | ✅ | ① `发布包\space_demo\requirements.txt` **补 `pydantic==2.10.6`**（否则 **D5 上线时 HF Spaces 全新容器装最新 pydantic，同一个崩会原样复现**）；② **在干净环境 `demo` 里做了单变量对照实验，把它从"推断"升级成"实测复现 + 实测修复"**（`2.12.0` 复现 → `2.10.6` 通过）；③ day18 教程补「**❗ 装依赖必看：别把 `requirements.txt` 跑进 `llm`**」与「**❗ 建环境连不上（换清华镜像）**」两整节（含金标准版本表、真冒烟命令、恢复命令清单）；④ FAQ 新增 6 条 |
| **`start.py` 指向 day18 完成版**（day17 待办④ / 教程 5.4） ⭐ | ✅ | `day17\start.py` 第 52 行 `..\day16\app.py` → **`..\day18\app.py`**，并同步更新文件头注释与 docstring。**已验证**：`python start.py --check` 打印 `[OK] Web 界面：…\day18\app.py` |
| **git 存档** | ✅ | **已按"分两笔"完成主体**：① `2660b33` **Day18 主体**（day18 教程 + day18 工作汇报 + `实验日志.md`，3 files / +365 / −20）；② `6aa8cd7` **F1 更正独立一笔**（day17 教程 + day17 工作汇报，2 files / +577 / −259）。后经 `27bd6e2` / `01f2d97` / `e703713` / `6ac1de9` 四笔收尾（回填 hash、日期口径、日志 Day18 小节、**R3 结果目录入库**），本次再补一笔 R3 解读与防线。⚠ 遗留：`171bca3` 与 `62a582b` 两条**同 message** 的旧提交仍在外层历史里，**建议 squash 但未做**（见第六节待补项 6） |

> **今日核心收获**：① **把"错在哪"分成了两层并各用一次实验钉死**——**库侧无硬错**（自检 9/10 自洽、0 错位、0 缺失）→ **查询侧是主瓶颈**（中文查询 40% vs 英文理想查询 100%）；② **一次负结果的完整归因**（RRF 帮倒忙的三个机制），比"涨了几个点"更能在面试里站住；③ **F 线从"300 条合成数据"变成"90 条可追溯、可验证、不撞评测题的论文真实数据"**——直接回应 HR 意见③；④ **D 线有了"能拿来用"的形态**（上传自己的 PDF → 立即可问 → 还能看指标对比）；⑤ **最重要的一条纪律收获**：**"多跑投票"防的是随机误差，"逐行对账"防的是人为笔误**——尺子本身不会算错，错的是把数字搬进文档的那一步；⑥ **一条工程纪律的实物证据**：`pip install -r requirements.txt` **是一条会改环境的写操作**，跑错环境 = 41 个包降级 + 半天恢复 → 于是**把"环境隔离"从一句口号变成 `conda create -n demo` + `requirements.txt` 里那行 `pydantic==2.10.6`**。

### 3.2 今日关键数字（六条线）⭐ 今天最重要的产出

> **口径声明（重要）**：下面 **A/B/C 三条是"诊断数字"，只用于定位瓶颈，不进「一、实验记录表」、不对外当成绩引用**；**D、F 两条是"可引用成绩"**（D 为机器校验产物，F 为 `eval_v2.py` 标准口径的完整成绩行）；**E 条不是实验数字，而是对自己证据链的一处更正**。这是今天刻意做的区分——避免把"我手工写的理想查询拿到 100%"说成"系统提升到 100%"。

**A. O1-R8 数据自检（卷子对不对）**

| 判定 | 条数 | 含义 |
|---|---|---|
| ✅ **标注自洽**（关键词能在标注段落里字面命中） | **9/10** | 检索没召回它 → **真失败** |
| ⚠ 疑似**标注错位**（关键词只在别处） | **0/10** | —— |
| ❌ 标注 **chunk 不存在** | **0/10** | —— |
| ❓ **需人工读原文**（多为中英翻译题） | **1/10**（Q8） | 人工读 chunk 3 → **标注正确** |

→ **一句话结论**：`source_chunk` 标注可信 → **检索未命中属真实失败，瓶颈在检索侧，不在标注侧**。

**B. O1-R5 三通道筛查（plain：中文问句，即现状）** — `retrieval_lab.py --top-k 8`

| 通道 | strict | loose | 命中的题号 |
|---|---|---|---|
| `vector`（Chroma + bge） | **4/10 = 40%** | 5/10 = 50% | **{3, 5, 9, 10}** |
| `bm25`（自带实现） | **4/10 = 40%** | 4/10 = 40% | **{1, 4, 7, 10}** |
| `hybrid`（RRF，`rrf_k=60`） | **3/10 = 30%** ⬇ | 5/10 = 50% | {5, 9, 10} |
| **vector ∪ bm25** | **7/10 = 70%** | —— | **{1,3,4,5,7,9,10}** |

→ **三点**：① **融合反而变差**（4/10 → 3/10，**丢了 Q3、一条没多**）→ 负结果，三个机制见 3.5；② **两通道高度互补**（并集 7/10 = **70%**，是任一单通道的 1.75 倍）；③ **失败模式互补**——`vector` 丢的全是**专名题**（Q1/Q7），`bm25` 丢的全是**中文题**（Q2/Q5/Q6/Q9 直接 `top=[]`）。

**C. O1-R4 HyDE 冒烟（英文理想查询 = 上界）** — `retrieval_lab.py --top-k 8 --query-mode hyde`

| 通道 | strict | loose |
|---|---|---|
| `vector+hyde` | **10/10 = 100%** | 9/10 = 90% |
| `bm25+hyde` | **10/10 = 100%** | 9/10 = 90% |
| `hybrid+hyde` | **10/10 = 100%** | 9/10 = 90% |

→ **同一套库、同一套判据，只换查询的语言与形态，strict 从 40% 直接到 100%（三通道一致）**。
> ⚠ **这是"上界"不是"成绩"**：查询是**人工写的理想查询**（等于开卷考试），只能用来**定位瓶颈**。真实现（模型自动改写）会低于上界——**那才是 Day19 要计入成绩的事**。

**D. F1 论文真实 QA（可引用成绩 · 机器校验）** — `build_paper_qa.py --min 50 --show-categories`

| 校验项 | 结果 |
|---|---|
| 实到条数 / 门槛 | **90** / 50 ✅ |
| chunk 库规模 | 105 ✅ |
| **逐字回查**（`chunk_id` 存在 + `quote` 逐字比对） | **通过 90 条** ✅ |
| **数据泄漏**（阈值 0.7，与 `eval_questions.json` 比） | **命中 0 条** ✅ |
| 硬错误（条数/字段/空值/重复/泄漏/可追溯） | **0 条** ✅ |
| 五类分布 | 术语定义 **18** / 方法步骤 **22** / 实验设置 **17** / 结论 **25** / 局限 **8** |

→ **可如实标注为"论文真实 QA，每条可追溯，且不撞评测题"**——这是对 HR 意见③最直接的回答。

**E. ⚠ 证据链数据错误的自查与修正（今天最该被记住的一件"小事"）**

| 项 | 内容 |
|---|---|
| **错在哪** | R1R2 的 **F1** 原写作 **0.889**，但 **0.889 是「精确率 P」**。脚本自己的输出（`day17\result_lora_k8_qi_runs3\run_log.txt` 第 160 行）是 `⑤ 防幻觉F1 : P=0.889 R=0.800 F1=0.842（TP=8 FP=1 FN=2 TN=9）` |
| **根因** | 撰写 day17 报告时把混淆矩阵写成了 day16 的 **FP=0 / TN=10**（正确应为 **FP=1 / TN=9**）。`FP=0` → `P=8/(8+0)=1.000` → `F1=2·1.0·0.8/1.8=0.889`——**谬误自洽地"算"出来了** |
| **真值** | **F1 = 0.842**（P=0.889, R=0.800, TP=8 **FP=1** FN=2 **TN=9**） |
| **影响面** | k 的贡献从"F1 **+0.264**"修正为 **+0.217**；相对基线 B0r3 的增量从"+0.183"修正为 **+0.136**。**结论方向不变**（R1R2 的 0.842 仍是最好，仍略高于 day16 R2 的 0.824），但幅度必须按 0.842 说 |
| **已同步** | `实验日志.md`（F1 列 + 结论文字 + 新增更正说明）/ `day17工作汇报.md`（5 处）/ `day17教程`（7 处）/ `day18教程`（4 处）；`day18\app.py` 读日志、**自动跟随**（实测 `F1 0.706 → 0.842 (+0.136)` ✅） |
| **顺手清理** | 日志表内原有**一行重复的 R1R2 占位行**（结论列是 `命中 X→Y，生成 X→Y` 模板文本，属 `--append-log` 试跑残留）与一行空行，已删除。**其 F1 值 0.842 反而是对的**——本次修正的依据正是它与 `run_log.txt` 的相互印证 |
| **教训（面试可直接讲）** | **"多跑投票"防的是随机误差，"照抄混淆矩阵"防的是人为笔误。** 尺子本身不会算错，**错的是把数字搬进文档的那一步**。这类错误**只能靠"文档数字 ↔ 脚本原始输出"逐行对账**来抓 → **Day18 起：对外引用的每个 F1，都要回 `run_log.txt` 对一次 TP/FP/FN/TN** |

**F. R3 确认跑（可引用成绩 · 完整对比行）** — `eval_v2.py --top-k 8 --runs 3 --exp-id R3`，结果目录 `day17\result_lora_k8_runs3\`

| 指标 | R3（**qi=off + k=8**） | R1R2（qi=on + k=8，仅作对照） | B0r3（qi=off + k=4，校准基线） |
|---|---|---|---|
| 检索 strict | **4/10 = 40%** | 3/10 = 30% | 2/10 = 20% |
| 检索 loose | **5/10 = 50%** | 4/10 = 40% | 3/10 = 30% |
| 生成正确率（in） | 4/10 = 40% | 4/10 = 40% | 2/10 = 20% |
| 防幻觉正确率（out） | 7/10 = 70% | **8/10 = 80%** | 6/10 = 60% |
| 防幻觉 F1 | 0.824（TP7 **FP0** FN3 TN10） | **0.842**（TP8 FP1 FN2 TN9） | 0.706（TP6 FP1 FN4 TN9） |
| 总正确率 | 11/20 = 55% | **12/20 = 60%** | 8/20 = 40% |
| 生成一致率 | 0.950 | 0.967 | 0.950 |

**→ 三条要点（引用时按这个口径讲）：**

1. **规格符合度：预测全中** —— 预测 strict 4/10、loose 5/10，实测 **4/10 / 5/10**；且与 day16 R2（同配置）**逐题 Top-8 序列与 20 条判定完全一致** → **这不是"抽到了一次好数"，是配置本身决定的稳定水平**。
2. **机制闭环**：回归的正是 **Q3**（前缀把它期望段 chunk 6 挤出前 8；关掉后 `top_ids=[100,36,103,97,101,6,98,91]`，chunk 6 回到第 6 名）→ **day17 那句"前缀的伤害是排名位移、k=8 不能完全掩盖"被后续实验验证**。
3. **⚠ 不能写成"R3 最好"**：R3 总分 55% / F1 0.824 **低于** R1R2 的 60% / 0.842，但差异**只来自 2 个格子**：**Q5**（in）R1R2 拒答 → R3 作答（**R3 修掉了那个 FP**）、**Q11**（out）R1R2 拒答 → R3 编造（**R3 多了这个 FN**）。且 **R3 是唯一 `FP=0` 的一轮**（没有"该答却拒答"）；Q11 在 R3 里三次一致（**稳定编造，不是抖动**）、它是 out 题、上下文同样随检索变动 → **这 1 题的摆动不足以支撑"前缀有助于防幻觉"**。→ **工程结论仍是 `qi=off + k=8`**（检索层机制明确，外加 FP=0），并**如实报出"总分与 F1 比带前缀那轮低 1 题"**，不挑好看的讲。

> **一句话总结今天**：**"卷子没问题（9/10 自洽）→ 库里有答案（关键词都在）→ 换英文问就全对（40%→100%）"**——三步把瓶颈从"检索参数"钉到了"**查询侧的语言错位**"。同时**在自己写的报告里抓出一个数据错误**，并把"对账"变成了固定动作；最后**用 R3 把 day17 的推测补成了实测**，也**用一次真实的环境事故证明了"环境隔离"不是口号**。

### 3.3 零散时间任务（0.5~1h）

> ⚠ **如实标注**：教程里排了力扣 198、统计八股 6 节串线①、牛客银行/央企真题 0.5 套三项，**但这三项没有产生可核验的产物（无提交记录、无笔记文件）**——**本报告不代填、不计入成果**，请在补齐后自行补记。

| 任务 | 完成情况 | 说明 |
|---|---|---|
| 力扣 198. 打家劫舍（一维 DP「选/不选」） | 确认 |
| 统计八股：6 节串线① | 确认 |
| 牛客银行/央企真题 0.5 套 | 确认 |

### 3.4 今日验收清单（对照教程勾选）

**第 0~1 步（决策门落地）：**

- [x] 默认检索配置已固化：**k=8 + 无前缀 + patch=on**；`--query-instruction` 恢复默认 `off`
- [x] **日期口径已统一（Day17 待办①，已关闭）**：口径选定为 **「日期 = 实际执行日」** → `实验日志.md` 中 B0r3 / R1 / R1R2 三行的 `9/21` **已改为实际执行日 `9/19`**（依据 `run_log.txt` 时间戳 15:17 / 15:31 / 16:28），并在表头上方补写「**日期口径**」说明（并注明"五轮同日 = 一次长会话，轮次靠编号区分"）。⚠ 说明：本报告初稿把这一条列为"未做"，是**报告写完后才补做的**，此处如实更新
- [x] **R3 确认跑已完成**（`eval_v2.py --top-k 8 --runs 3 --exp-id R3`）→ 结果目录 `day17\result_lora_k8_runs3\`；**strict 4/10、loose 5/10、生成 4/10、防幻觉 7/10、F1 0.824、总 11/20、一致率 0.950**，已 `--append-log` 入「一、实验记录表」并回填结论列。**注意**：这条**证实的正是 day17 的机制推断**（前缀把 Q3 的 chunk 6 挤出前 8），**并且**它是**对外引用 k=8 配置的正式一行**（`retrieval_lab.py` 只能替代其中的"检索 strict"这一项，不能替代生成/防幻觉/F1）

**第 2 步（O1-R8 数据自检）⭐：**

- [x] `data_selfcheck.py` 跑通，看到 **9/10 自洽、0 错位、0 缺失、1 需人工**
- [x] 能说出三种"假失败"的区别（标注错位 / chunk 缺失 / 翻译题），以及各自该怪谁
- [x] 用 `--dump-chunk 3` 亲眼确认 Q8 的英文原句（"GMR consistently outperforms existing open-source methods…"），**人工判定标注正确**
- [x] 用 `--dump-chunk 0 5` 确认 Q10 标注正确（`bone length` / `joint range of motion` / `mass distribution` 均在 chunk 5）
- [x] 能说出"程序只做字面匹配、**翻译题必须人工核对**"这条边界
- [x] 报告里已写出一句话结论：**标注可信 → 检索未命中是真失败**

**第 3 步（O1-R4 HyDE + O1-R5 混合检索）⭐⭐：**

- [x] `retrieval_lab.py --top-k 8` 跑通：**vector 4/10、bm25 4/10、hybrid 3/10**
- [x] 能说出**两通道命中集合互补**（`vector∪bm25 = 7/10 = 70%`）
- [x] 能解释 **BM25 为什么在中文查询下大面积 `top=[]`**（只吃 ASCII token，中文问句对它近乎零信号）
- [x] `--query-mode hyde` 跑通：三通道 **strict 10/10**
- [x] 能强调 **HyDE 的 10/10 是"上界"不是"成绩"**（人工理想查询 = 开卷考试）
- [x] 能讲出 **RRF 帮倒忙的三个机制**（零信号等权融合 / `1/(60+rank)` 名次压缩 / 候选池质量不对等）
- [x] 记住交叉验证：`retrieval_lab` 的 vector = **4/10**，与 day16 R2 一致
- [x] 已决定 Day19 主路线：**模型自动改写（HyDE 落地）→ 再融合 / 或直接上 Rerank**

**第 4 步（F1 论文真实 QA）⭐⭐：**

- [x] `sft_data_paper.json` 已达 **90 条**（计划 ≥50）
- [x] `build_paper_qa.py --min 50` 全绿：**逐字回查 90/90、泄漏 0、硬错误 0**
- [x] 五类分布齐备：**18 / 22 / 17 / 25 / 8**
- [x] 每条都带 `source.page` + `source.chunk_id` + `source.quote`（可追溯）
- [x] 已确认**不撞评测题**（泄漏检测命中 0 条）

**第 5 步（D2 Web UI 完成版）：**

- [x] `python app.py --check` 通过（`[check] 界面构建成功（未启动服务）`）
- [x] 指标对比表**从 `实验日志.md` 现读**（改日志即改显示，实测 `F1 0.706 → 0.842 (+0.136)`）
- [x] **不上传、不建库也能直接用默认 GMR 库**（`resolve_store()` 回退逻辑，bug 已修）
- [x] 上传 PDF 建的是**内存库**（关页面即释放，不落盘、不污染 day13 库）
- [x] 召回片段可视化 + Top-K 可调 + 双模型切换 + 服务身份检查
- [x] **教程 5.4「把 `start.py` 指向 `day18\app.py`」已完成**——`day17\start.py` 第 52 行路径常量已改，文件头注释同步更新；**已验证**：`python start.py --check` 打印 `[OK] Web 界面：…\day18\app.py`

**第 6 步（发布包骨架）：**

- [x] **6.3 静态核对通过**（不花额度也能做，已实跑留下一串可核验输出）：`Select-String -Path app.py,README.md -Pattern "sk-","api_key"` **只在注释与 README 的占位符里命中，代码里无硬编码 key**；密钥全部走 `os.environ.get("API_KEY")`；三处标注（页面 `DEMO_NOTE` / `README.md` / `..\blog\项目展示页.md`）齐备；端点 `api.deepseek.com/v1/chat/completions` 与 `MODEL_NAME=deepseek-chat` 正确；发布包体积 **13,130 B ≈ 12.8 KB < 20 MB**（无权重 / adapter / 大向量库）
- [ ] ⚠ **6.2「真冒烟」未做**（不上线、但要花 DeepSeek 额度）→ **如实记为未跑**。已做的是：`pip install -r requirements.txt` **在 `llm` 环境里被误执行**（就是第 12 条事故的起点），**正确做法（独立环境 `demo`）已写进教程 6.2 的红字前置警告**，正在按它重来
- [x] **D5 上线前必补项已提前做掉并验证**：`发布包\space_demo\requirements.txt` **补 `pydantic==2.10.6`**；并在干净环境 `demo` 里**单变量对照验证**（`pydantic 2.12.0` → 复现 `TypeError`；`2.10.6` → `OK, 接口数 = 2`）——**不是"应该能修好"，是"实测修好了"**

**睡前：**

- [x] `git check-ignore` 复核：`第三周\day13\chroma_db\`、`第三周\day12\lora_adapter\`、`download/` 三条**均有输出**（被正确忽略）
- [x] 提交：Day18 相关共 **6 笔**（`2660b33` / `6aa8cd7` / `27bd6e2` / `01f2d97` / `e703713` / `6ac1de9`），**工作区已干净**（`git status` 无输出）；R3 结果目录 `day17\result_lora_k8_runs3\` 已入库（`eval_results.json` / `评测表.md` / `run_log.txt`）；临时文件 `day18\_gmr_all_chunks.txt` 已清理

### 3.5 当天逐个查清的关键问题（都留了实证）

- **"检索命中率上不去，会不会是评测题的 `source_chunk` 标注错了？"** → **先查卷子，再怪检索**。`data_selfcheck.py` 实测 **9/10 关键词能在标注段落内字面命中、0 条错位、0 条 chunk 缺失**；唯一"需人工"的 Q8 用 `--dump-chunk 3` 打原文确认**标注正确**（关键词是中文概括，属**翻译题**不是标注错）。→ **结论：标注可信，检索未命中是真失败。**（这一步的价值是**把归因方向钉死**，不是提升任何数字。）
- **"为什么程序查不到 Q8 的关键词，却不能说标注错了？"** → 因为脚本**只做字面匹配**。它证明的是"**关键词的字面形态**出现过"，**不能替你做语义判断**。所以报告里必须写"**经人工读原文核对后确认**"，而不是"程序证明标注都对"。**这个区分要写清楚，否则就是拿工具的输出冒充结论。**
- **"40% 的命中率，到底是库不行还是问得不对？"** → 用一个**便宜的对照实验**分开：同一套库、同一套双轨判据，**只把中文问句换成英文术语**（`--query-mode hyde`），strict **40% → 100%（三通道一致）** → **信息在库里，是查询端对不上**。**假设②成立，且量级很大。**
- **"那是不是说明 HyDE 能提升到 100%？"** → **不能这么写。** 我用的是**人工写的理想查询**，等于开卷考试——它只能回答"**瓶颈在哪**"，不能回答"**能提升多少**"。真实现（用本地 3B 自动改写）会因为**改写本身出错**而低于上界，**那才是要计入成绩的数字**。→ **Day19 把 HyDE 做成"模型自动改写"再跑一遍。**
- **"向量和 BM25 都 4/10，混合检索应该更强吧？"** → **实测反而掉了**（4/10 → 3/10，丢 Q3、一条没多）。三个机制：① **BM25 对中文查询近乎零信号**（Q2/Q5/Q6/Q9 直接 `top=[]`），而 **RRF 仍给它等权**，等于给有效排序掺一半噪声；② **`1/(60+rank)` 把名次差压平**（第 1 名 `1/61≈0.0164` vs 第 8 名 `1/68≈0.0147`，**只差 11%**），向量"第 1 名很确信"的强信号被稀释；③ **RRF 只看名次、不看"这一路可不可信"**。→ **结论不是"混合检索没用"，而是"得先解决查询侧的语言错位，融合才有意义"**——**反证**：hyde 模式下两路都有信号，三通道全部 10/10，**融合不再有害**。
- **"两个通道都只有 4/10，是不是说它们都很弱？"** → **看并集，不看单通道**：`vector ∪ bm25 = 7/10 = 70%`，**是任一单通道的 1.75 倍**；且**失败模式互补**（vector 丢专名题 Q1/Q7，bm25 丢中文题 Q2/Q5/Q6/Q9）。→ **这既是"混合检索有前途"的证据，也是"必须先改写查询"的证据**——因为 BM25 在中文查询下基本是瞎的。
- **"`retrieval_lab` 的 vector 是 4/10，这和 day16 R2 的 4/10 是巧合吗？"** → **不是巧合，是一次独立交叉验证**：`retrieval_lab` 的 vector = **无前缀 + k=8 + 同一双轨 strict 判据**，正好等于 day17 决策门里"**关掉前缀、只留 k=8 大概率回到 4/10**"那个**推测**的条件 → 实测 **4/10，与 day16 R2 完全一致** → **推测被证实**。→ **k 这条腿可以彻底放下了**（但它**只是"完整成绩行"的替代证据**——含生成/防幻觉/F1 的正式一行仍需 `eval_v2.py` 补跑）。
- **"day17 报告里的 F1 0.889，为什么会被写错？"** → 根因是**抄错了混淆矩阵的一个格子**：把 day16 的 **FP=0 / TN=10** 抄到了 R1R2 行（实际 **FP=1 / TN=9**）。`FP=0` → `P=8/(8+0)=1.000` → `F1=2·1.0·0.8/1.8=0.889`，**看起来完全自洽，所以没人会怀疑**。→ **抓它靠的不是"再算一遍"，而是"拿文档数字去对脚本原始输出"**（`run_log.txt` 第 160 行）。**这类错误比随机波动危险得多**：波动会让数字跳，**笔误会让数字稳定地错**。
- **"报告里发现的错误，为什么值得单列一节？"** → 因为**证据链的价值不在"数字好看"，而在"数字可被复核"**。面试官随手抽一行去对 `run_log.txt`，能对上，这份《优化成果报告》才可信。→ **把它写进报告反而是加分项**：说明我**知道怎么给自己的工作做质检**，而不是只会跑脚本。
- **"R3 的 strict 和 `retrieval_lab` 的 4/10 一样，那 `retrieval_lab` 是不是就够了、不用跑 `eval_v2`？"** → **不够，但两者互补**。`retrieval_lab.py` **只做检索、不做生成**，它能证明"**检索这一项**回到了 4/10"，却**给不出生成 / 防幻觉 / F1**——而《优化成果报告》要的是**完整对比行**。→ **R3 是"正式一行"，`retrieval_lab` 是"便宜的诊断证据"，两者数字吻合（4/10 ↔ 4/10）反而互相印证了对方的可靠性。**
- **"R3 的 F1（0.824）比 R1R2（0.842）低，那你为什么还选 `qi=off`？"** → 因为差的**不是"前缀有用"，而是 2 个格子**：**Q5**（in）R1R2 拒答 → R3 作答（**R3 修掉了 1 个 FP**）、**Q11**（out）R1R2 拒答 → R3 编造（**R3 多了 1 个 FN**）；**且 R3 是唯一 `FP=0` 的一轮**。Q11 在 R3 里三次一致（**稳定编造、不是抖动**），它是 out 题、上下文同样随检索变动 → **这 1 题的差异不足以支撑"前缀有助于防幻觉"**；而前缀有害在**检索层有明确机制**（Q3 的 chunk 6 被挤出前 8，关掉即回归）。→ **选 `qi=off`，同时如实报出"总分与 F1 各低 1 题"**，不挑好看的讲。
- **"为什么要把'我把自己的环境搞坏了'写进报告？"** → 因为这次事故的根因不是"手滑"，而是两条**可复用的判据**：① **`pip install -r` 会改环境**——跑之前先问"这份清单是给谁的环境用的"；② **"服务能起" ≠ "服务能用"**——`--check` 只构建界面、不碰请求路径，必须用 `.get_api_info()` 真冒烟。→ **把判据写下来，比"我以后小心点"有用得多**；顺带也解释清了**为什么 `requirements.txt` 必须 pin `pydantic==2.10.6`**（否则 D5 上线时 HF Spaces 全新容器会原样复现）。
- **"你怎么确定是 pydantic 的问题，而不是 gradio / 别的包？"** → **不靠猜，靠单变量对照**：在干净的新环境 `demo` 里，**同一份 `space_demo\app.py`、同一条命令，只改 `pydantic`**——`2.12.0` 准点复现 `TypeError`（栈帧精确落在 `gradio_client\utils.py:863  if "const" in schema:`，**和事故现场一模一样**），换回 `2.10.6` 同一命令立刻 `OK, 接口数 = 2`。→ **根因不是我根据报错推断的，是被复现出来的**；这也顺带证明了那个 `pin` 是真修复、不是碰巧。
- **"独立环境跑通了，说明什么？"** → 说明**"环境隔离"这个纪律是有具体收益的、可度量的**：同一个 `pip install -r requirements.txt`，**在 `llm` 里跑 = 41 个包降级 + 服务 500 + 半天恢复**；**在 `demo` 里跑 = 一次干净的成功**（`llm` 侧的 `gradio 6.27 / pydantic 2.13.4` **事后复查一字未动**）。→ **结论不是"以后小心点"，而是"用一次 `conda create` 买断这类风险"**。

---

## 四、踩坑与解决（过程真实记录）

| # | 问题 | 解决办法 |
|---|---|---|
| 1 | **`UnicodeEncodeError` / 控制台中文乱码**（中文路径下跑脚本、`Select-String` 输出、重定向落盘都出问题） | 输出端 UTF-8 vs 显示端 GBK 不匹配 → `chcp 65001` + `$env:PYTHONIOENCODING='utf-8'`；脚本内显式 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`。**重定向落盘时更麻烦**（PowerShell `>` 会写成 UTF-16），改用 `Out-File -Encoding utf8` 或让 Python 自己写文件 |
| 2 | **`ModuleNotFoundError: No module named 'langchain_chroma'`**（跑 `retrieval_lab.py`） | 用了默认解释器而不是 `llm` 环境 → **必须显式调用** `& "D:\miniconda1\envs\llm\python.exe"`（或用 `conda activate llm`）。**这是今天最常踩的一个坑** |
| 3 | **BM25 通道全 0 命中**（strict 0/10、loose 0/10） | 根因：`BM25.score()` 把**原始文本** `texts[i]` 传给了内部的 `Counter`，而不是**预分好的 token 列表** `doc_tokens[i]`。修正 `self.score(q_tokens, i, texts[i])` → `self.score(q_tokens, i)` 后正常 |
| 4 | **PowerShell 报 `MissingEndSquareBracket`**（把 Python 脚本塞进命令行时） | PowerShell 会解析字符串字面量里的 `[` / `@` → **改成写临时 `.py` 文件再跑**，或用 here-string `@'...'@` 管道给 python |
| 5 | **`build_paper_qa.py` 报"条数不足"** | 不是 bug：校验器默认 `--min 50`，而当时只有 5 条种子数据 → **把数据补到 90 条**后通过 |
| 6 | **9 条 quote 逐字回查失败** | 三个 PDF 坑（已写进教程 4.5.1）：① **quote 跨 chunk** → `chunk_id` 改成**列表**（如 `[13, 14]`）让校验器拼接比对；② **PDF 断词连字符**（`per-form`）→ 调整 quote 避开或按原文形态写；③ **智能引号**（`’` U+2019）→ `norm()` 只去掉了直引号，需按原字符匹配 |
| 7 | **`uvicorn` 起不来（`No module named uvicorn`）** | 用户在 `(base)` 环境 → **必须先 `conda activate llm`**（`uvicorn` 装在 `llm` 里）。已写进教程「起服务必看」小节 |
| 8 | **`app.py` 不上传 PDF 就报 `store is None`** | `_on_build()` 没把 `store` 回传给 `store_state`，且 `ask()` 见 `None` 即报错 → ① `_on_build` 返回 `store`；② `build_btn.click` 的 `outputs` 加上 `store_state`；③ 新增 `resolve_store()` 在 `None` 时**回退默认 GMR 库** |
| 9 | **`app.py` 的指标表把 F1 解析错、且选错基线** | `_pct()`（百分比）与 `_num()`（小数）混用；基线选成了 `B0`（runs=1）而非 `B0r3`（校准后基线）→ 修正解析函数；基线优先 `B0r3`、且**从"最优"候选里排除基线本身**；另加"`runs` 数不同的行不可比"的告警 |
| 10 | **`实验日志.md` 里 R1R2 的 F1 写作 0.889** ⭐ | **根因是混淆矩阵抄错**（`FP=0/TN=10` ← 应为 `FP=1/TN=9`）。**修正为 0.842**，并同步 4 个文档 + 清理一行重复占位行 + 新增更正说明。**教训：文档数字必须回 `run_log.txt` 对账** |
| 11 | **day17 报告里的"参考列"仍不可直接相减** | day16 R2 是 `runs=1`、今天三组是 `runs=3`，**投票本身会改数字** → 报告里继续保留"参考列仅看方向"的标注（这条纪律今天继续有效） |
| 12 | **收尾时 `eval_v2.py` 命令直接报 `ParserError: "<"运算符是为将来使用而保留的`** ⭐ | 教程里的占位符 `--date <你的实际日期>` 被**原样粘进 PowerShell**——`<` 是 PowerShell 的保留运算符，**python 根本没启动**。→ 换成 `--date 9/20` 即可。**判据：报错里有 `CategoryInfo: ParserError` 就是 shell 层，别去改脚本** |
| 13 | **`pip install -r requirements.txt` 把共享的 `llm` 环境打坏（41 个包降级）** ⭐⭐ 今天代价最大 | **这不是代码错，是环境错**：那份清单是给 **HF Spaces 全新容器**用的，会主动降级 `gradio / langchain / chromadb / transformers / numpy`。→ **正确姿势**：`conda create -n demo python=3.11 -y` 后在里面装；**教训**：`pip install -r` 是"会改环境的写操作"，跑之前先问"这份清单是给谁的环境用的" |
| 14 | **降级后 `app.py` 起不来：`TypeError: got an unexpected keyword argument 'theme'`** | `app.py` 的 `launch(theme=gr.themes.Soft())` 是给 **gradio 6.x** 写的；4.44.0 的 `launch()` 没这个参数（必须放 `Blocks(theme=...)`）→ 靠**恢复环境**解决，而不是改代码迁就旧版 |
| 15 | **降级后"服务能起、但每个请求都 500"，日志刷 `TypeError: argument of type 'bool' is not iterable`** ⭐ 最隐蔽的一个 | **根因是 `pydantic ≥2.11` 的 Schema 变更**：`dict` 字段的 JSON Schema 由 `additionalProperties: {}` 变成 `true`（布尔），而 `gradio_client 1.3.0` 的 `get_type()` 写的是 `if "const" in schema:` → **拿布尔值做 `in` 直接抛错**；首页与 `/api/info` 都要走它。**⚠ 关键认知**：`gradio 4.44.0` 只写 `pydantic>=2.0`（**无上界**），**pip 不会报任何冲突** → 这类"依赖声明过松"的坑，只能靠 `.get_api_info()` 真冒烟来抓（`--check` 查不出来，因为它不碰请求路径）。**✅ 收尾时在干净环境里做了受控实验把它"实测复现 + 实测修复"**（见 #20） |
| 16 | **恢复时又撞 `ERROR: No matching distribution found for langchain-community==0.4.2`** ⭐ 容易误判 | **不是版本不存在**（清华镜像上 24 个版本实测 24/24 全在）——是 **pip 读不到 `pypi.org` 的索引页**（`SSLEOFError`，报错前有 `Could not fetch URL ... - skipping`）。→ `pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple` 后同一命令即成功。**判据：pip 说"没有匹配的发行版"但没列 `from versions:`，八成是网络/索引问题** |
| 17 | **`localhost is not accessible` / `share=True` 的误导** | 这是 15 的**连锁反应**：`launch()` 会探 `127.0.0.1:7860/` 确认服务起来，而首页正被那个 `TypeError` 打成 500 → 探测失败 → gradio **误判"本机不通"**。→ **修 15 即可**，不要去开 `share=True`、也不用查代理 |
| 18 | **按正确姿势开独立环境时，第一步 `conda create -n demo python=3.11 -y` 又挂了**：`CondaSSLError` / `SSLEOFError` / `read timeout=5` ⭐ | **不是命令错，是连不上 `repo.anaconda.com`**。⚠ 关键在于**别被后面两条报错带偏**：环境没建出来 → `conda activate demo` 报 `EnvironmentNameNotFound`，紧跟的 `Invoke-Expression ... 参数为空字符串` 只是 conda 的 PowerShell 钩子在激活失败时的连带报错 —— **三个报错 = 一件事**。→ 让 conda 也走清华镜像（改 `D:\miniconda1\.condarc`，并把读超时 5s → 120s、关掉 notices 噪音），`conda create` 约 1 分钟建成（`demo = D:\miniconda1\envs\demo`，Python 3.11.16） |
| 19 | **改完 `.condarc` 后 conda 全线炸：`UnicodeDecodeError: 'gbk' codec can't decode byte 0x8e`** ⭐⭐ 这个坑是我自己造的 | **`.condarc` 必须纯 ASCII**：中文 Windows 上 conda 用 **GBK** 读它，**我在里面写了中文注释**→ 整份配置读不动，连 `conda --version` 都报错，还伪装成"conda 坏了"。→ 改成**英文注释**即恢复；**自查一行**：非 ASCII 字节数必须为 0。**教训：给"配置文件"写注释前，先确认解析它的人用什么编码。** |
| 20 | **受控实验：把"pydantic 根因"从推断做成实测复现 + 实测修复** ⭐⭐ 今天证据链上最干净的一对 before/after | 在**干净的新环境 `demo`** 里，同一份 `space_demo\app.py`、同一条命令，**只改 `pydantic` 一个变量**：<br>① `pydantic==2.12.0` → **复现** `TypeError: argument of type 'bool' is not iterable`，且栈帧精确落在预测位置：`gradio_client\utils.py:863  if "const" in schema:`；<br>② `pydantic==2.10.6` → **`OK, 接口数 = 2`**。<br>→ **pin 不是"试试看"，是实证过的修复**；也直接证明 #15 的机制判断（`additionalProperties` 由 `{}` 变 `true`）**是对的**，而不是我根据报错猜的 |

---

## 五、当日产出文件（位于 `第四周\day18\`）

| 文件 | 说明 |
|---|---|
| `day18-数据自检与混合检索及论文真实数据详细教程.md` | 当日超详细教程（**约 1500 行**：0~6 步主线 + 零散时间 + 睡前收尾 + 常见问题速查 + 验收清单 + 附录），含「4.5.1 抄 quote 必踩的三个 PDF 坑」等按实跑回填的章节 |
| `data_selfcheck.py` | **核心产出（约 340 行）**：O1-R8 数据自检——三重核对（chunk 存在 / 关键词在标注段内 / 关键词只在别处）+ `--dump-chunk` / `--scan` / `SEMANTIC_HINT` |
| `retrieval_lab.py` | **核心产出（约 430 行）**：检索层单独筛查——`vector` / `bm25`（自带实现）/ `hybrid`（RRF）+ `--query-mode plain/hyde`，**判据与 `eval_v2.py` 双轨一致** |
| `retrieval_lab.json` | plain 模式（`--top-k 8`）三通道逐题结果（含每题 top_ids 与相似度分），本次修正的交叉验证依据之一 |
| `build_paper_qa.py` | **核心产出（约 460 行）**：F1 数据六查——条数 / 字段 / 空值重复 / **逐字可追溯** / **数据泄漏** / 五类分布；`chunk_id` 支持列表、`norm()` 归一化 |
| `sft_data_paper.json` | **核心产出**：**论文真实 QA 90 条**（术语定义 18 / 方法步骤 22 / 实验设置 17 / 结论 25 / 局限 8），**逐字回查 90/90、泄漏 0** |
| `sft_data_paper.seed.json` | 5 条种子示例（用于演示校验器与数据规格） |
| `app.py` | **核心产出（约 470 行）**：D2 Web UI 完成版——上传 PDF 建库 / 指标对比（现读日志）/ 召回可视化 / Top-K 可调 / 双模型切换 + 服务身份检查；含本轮 `store_state` 回退 bug 修复 |
| `day18工作汇报.md` | 本工作汇报 |

**`第四周\` 下同时更新**：`实验日志.md`——① 新增「9/20（Day18）实测结论」小节（三通道 × 两模式对照表 + 三条结论 + 默认配置与方向）；② 「三、失败尝试记录」补 **RRF 负结果 1 条**；③ 新增**数据更正说明**（F1 0.889→0.842 的错因、根因、影响面、已同步范围与教训），并清理一行重复占位行 + 一行空行；④ **「一、实验记录表」新增 R3 行**（`--append-log` 自动插入 + 结论列按实测回填），并在表下补「Day18 补跑确认项 R3」的五条说明（含"不要写成 R3 最好"的边界）。

**`day17\result_lora_k8_runs3\`（R3 产物，已入库）**：`run_log.txt`（含 `④/⑤` 指标行与混淆矩阵，是"文档数字 ↔ 脚本输出"对账的原始依据）、`评测表.md`（v2.1 格式，逐题 Top-8 与判定）、`eval_results.json`（结构化逐题结果，供后续脚本复算）。

**`day17\start.py`（已更新）**：`APP_PATH` 由 `..\day16\app.py` 改为 **`..\day18\app.py`**（完成版界面），文件头注释与 docstring 同步；`--check` 已验证指向正确。

**`发布包\space_demo\requirements.txt`（已更新）**：**新增 `pydantic==2.10.6`** 并附注原因（gradio 4.44 只写 `pydantic>=2.0` 无上界，pydantic ≥2.11 的 Schema 变更会让 `gradio_client` 抛 `TypeError` → 服务能起但每个请求 500）——**这是把今天的事故固化成 D5 上线的防线**。

**同时修正（历史文档数字对齐）**：`day17\day17工作汇报.md`（5 处）、`day17\day17-复跑基线与首轮检索实验详细教程.md`（7 处）、`day18\day18-…教程.md`（4 处）——全部 `F1 0.889 → 0.842`，混淆矩阵统一为 **`TP8 FP1 FN2 TN9`**。

**day18 教程同日回填**：① 1.3 节「预测表」改为**实测值**（并新增三条解读与"引用口径"说明）；② 6.2 节加**红字前置警告**（别把 `requirements.txt` 跑进 `llm`）+ 6.3 静态核实跑结果；③ FAQ 新增 4 条并在末尾加「**❗ 装依赖必看**」整节（含恢复命令清单）；④ 开头"5 条待办"里第 2 条改为 **✅ 双证据已完成**；⑤ 验收清单同步打勾。

**Git 存档**：✅ **已完成**（主体按"分两笔"提交，后续四笔收尾）。

| 提交 | 内容 | 规模 |
|---|---|---|
| `2660b33` | **Day18 主体**：`day18-…教程.md`（含 5.2.1 F1 更正小节 + 日期口径更新）、`day18工作汇报.md`（新建）、`实验日志.md`（Day18 实测结论 + RRF 负结果 + 日期口径统一 + F1 更正说明） | 3 files / **+365 / −20** |
| `6aa8cd7` | **F1 更正独立一笔**：`day17工作汇报.md`（新建，5 处更正）、`day17-…教程.md`（7 处更正），混淆矩阵统一为 `TP8 FP1 FN2 TN9` | 2 files / **+577 / −259** |
| `27bd6e2` | **本汇报收尾**：回填上面两笔 hash，并把「日期口径」「git 收尾」两条待补项据实关闭（**初稿写完后才做成的事，不装成一开始就完成**） | 1 file / **+13 / −5** |
| `01f2d97` | **日期口径收尾**：`实验日志.md` 失败尝试记录里 day16 那行 `9/16 → 9/19`（day16 实际也跑在 9/19），让全表口径一致 | 1 file / **+1 / −1** |
| `e703713` | **汇报再收尾**：补记 `27bd6e2` / `01f2d97` 两笔，并修正"明日安排"里已关闭的待补项 | 1 file |
| `6ac1de9` | **R3 结果与主线成果入库**：`day17\result_lora_k8_runs3\` 三件（`eval_results.json` / `评测表.md` / `run_log.txt`，共约 **1238 行**）+ `实验日志.md`（R3 行）+ `day18工作汇报.md` 小改（message 含数字：9/10 自洽 / 4-4-3 / HyDE 上界 10/10 / 90 条 QA） | 5 files / **+1242 / −3** |
| `49b7032` | **R3 入账**：`实验日志.md` 回填 R3 结论列（strict 4/10 证实"前缀伤害=排名位移"）+ 表下五条说明（含 FP=0、与 day16 R2 逐题复现、与 R1R2 的 2 格差异边界）+ 决策门标记"已闭环" | 1 file / **+10 / −2** |
| `ae7d89e` | **R3 的解读与防线**（本次收尾主体）：`day18教程`（1.3 预测表改实测 + 三条要点 + 引用口径 + FAQ 四条 + 「❗ 装依赖必看」整节 + 6.2 红字警告 + 验收清单打勾）；`start.py`（指向 `day18\app.py` 完成版）；`requirements.txt`（补 `pydantic==2.10.6`）；`day18工作汇报.md`（新增踩坑 12~17、3.2 加 F 条、3.4 / 五 / 六节同步） | 4 files / **+212 / −52** |

> 说明：**上面这两笔 hash 是在它们之后的一笔里回填的**（"回填自己的 hash"必然如此，除非把 hash 写进 commit message）。**本次到此为止，不再为这点自指问题再追一笔。**

→ **把"更正"独立成一笔提交，本身就是在证据链上留痕**：日后回看 `git log`，能清楚看到"哪一笔是更正了哪个数字"，而不用去猜。
→ **工作区已干净**（`git status` 无输出）。⚠ **但有两处遗留**：① `171bca3` 与 `62a582b` 两条**message 完全相同**的旧提交仍在历史里（后者实际含 `app.py` 修复与教程更新），**建议 squash 但本次未做**（非交互式环境下 squash 需 rebase，风险高于收益，故如实挂账）；② `6aa8cd7` 那一笔里，day17 教程**还夹带了一批既有未提交内容改动**（约 131/72 行，非本次更正产生）——已在 commit message 里注明，但**没能拆开**（同一文件内混在一起，除非做 hunk 级拆分）。

---

## 六、小结与明日安排

- **小结**：Day 18 目标达成——**把 Day17 的"猜测"做成了"证据"**。① **归因链完整**：`data_selfcheck`（9/10 自洽、0 错位、0 缺失）→ **排除标注错**；`retrieval_lab` plain（vector 4/10、bm25 4/10、hybrid 3/10、**并集 7/10**）→ **看清通道互补**；`--query-mode hyde`（三通道 **10/10**）→ **证明瓶颈在查询侧**。三步推出的结论是：**信息在库里，是中文问句对不上英文段落**。② **一条干净的负结果**：RRF 融合**反而掉一题**，并给出**三个机制**（零信号等权融合 / 名次压缩 / 候选池质量不对等），还留了**反证**（hyde 下融合不再有害）——**比"涨了几个点"更能撑住面试追问**。③ **F 线出了实体**：**90 条论文真实 QA，逐字可追溯、零泄漏、不撞评测题**，直接回应 HR 意见③。④ **D 线有了"能用"的形态**：Web UI 完成版**开箱即用**（不上传也能用默认库），且指标表**现读日志**而不是硬编码。⑤ **一条纪律收获**：**自查出 day17 报告里把"精确率"当成 F1 的笔误**（0.889 → **0.842**），**同步修正 4 个文档**，并把"文档数字 ↔ `run_log.txt` 对账"变成固定动作——**这说明我的证据链现在是可被复核的**。⑥ **收尾又补上两件**：**R3 确认跑**把 day17 的机制推断变成实测（strict 4/10、F1 0.824、唯一 FP=0），**k=8 无前缀这条配置终于有了可对外引用的完整成绩行**；**一次环境事故**（41 个包降级）从定位到恢复全走了一遍，并把根因固化成 `requirements.txt` 的 `pydantic==2.10.6` 与教程的"装依赖必看"红字——**事故本身不值钱，"把它变成防线"值钱**。
- **待补完成项（如实标注，均已写明去向）**：
  1. **✅ 日期口径已统一**（day17 待办①，**本报告初稿后补做**）：口径选定 **「日期 = 实际执行日」**，`实验日志.md` 中 day17 三行 `9/21` → `9/19`（`run_log.txt` 时间戳 15:17 / 15:31 / 16:28 为依据），表头补写口径说明。→ **本条已关闭。**
  2. **✅ R3 确认跑已完成**（`eval_v2.py --top-k 8 --runs 3 --exp-id R3`）：**strict 4/10、loose 5/10、生成 4/10、防幻觉 7/10、F1 0.824、总 11/20、一致率 0.950**，结果目录 `day17\result_lora_k8_runs3\`，已入「一、实验记录表」并回填结论列。**回归的正是 Q3**（前缀曾把它的 chunk 6 挤出前 8）→ **day17 的机制推断被实测验证**。→ **本条已关闭。** ⚠ 注意：**R3 的总分/F1 比带前缀的 R1R2 各低 1 题**（Q5 少一个 FP、Q11 多一个 FN），报告按此口径如实写，**不写成"R3 最好"**。
  3. **✅ 教程 5.4 已完成**：`day17\start.py` 第 52 行已改为 `..\day18\app.py`，注释同步；`python start.py --check` 打印 `[OK] Web 界面：…\day18\app.py`。→ **本条已关闭。**
  4. **6.3 静态核对 ✅ 通过**；**6.2 真冒烟 ⚠ 未做**（不花 DeepSeek 额度）：`pip install -r requirements.txt` 曾在 `llm` 里被误执行（引发第 12~17 条事故，已恢复并复验），**独立环境 `demo` 的正确姿势已写进教程 6.2 红字警告**。**收尾时又发现连"开独立环境"这第一步都过不去**（`conda create` 连不上 `repo.anaconda.com`，见第 18 条）→ **已修好 conda 镜像源并建成 `demo`（Python 3.11.16）**，`pip install -r requirements.txt` **一次干净跑通**（`pydantic 2.10.6` / `gradio 4.44.0` / `torch 2.14.0+cpu`，与清单完全一致；**`llm` 侧事后复查一字未动**：`gradio 6.27.0` / `pydantic 2.13.4`）。**并已用 `demo` 跑通 UI 冒烟**：`get_api_info()` 返回 **2**（这条正是抓 #15 那个 bug 的命令）。→ **本条按纪律挂账的方式更精确了**：**"环境已就绪 + UI 冒烟已过"✅，"DeepSeek 端到端问答（真实起页面、真实调 API）"⚠ 仍待做**（要 key、花额度）。
  5. **⚠ 零散时间三项（力扣 198 / 统计八股串线① / 牛客 0.5 套）无可核验产物** → 不代填，待自行补记。
  6. **✅ Git 收尾已完成**（Day18 相关共 7 笔，最新一笔含 R3 结果目录与本次收尾），工作区已干净。→ **本条已关闭**。**遗留**：`171bca3` / `62a582b` 两条同 message 的旧提交**建议 squash 但未做**（需 rebase，非交互环境下风险高于收益）。
  7. **✅ `实验日志.md`「一、实验记录表」今日已新增 R3 行**——含**结论列实测回填**与五条说明（含"不要写成 R3 最好"的边界、与 day16 R2 的逐题一致复现证据）。`retrieval_lab` 仍**不进正式成绩表**（诊断口径），这点没变。→ **本条已关闭。**
- **明日安排（D4，计划 9/23 周三 · day19）**：
  1. **把 HyDE 从"手工冒烟"做成"模型自动改写"** ⭐：用本地 Qwen2.5-3B 把中文问句改写成"英文术语 + 假设答案片段"再送检索，**用 `eval_v2.py` 跑全量 20 题、计入成绩**（改写会出错，这才是真实数字）；调不动就退回"轻量改写"（只抽英文术语，不生成假设答案）；
  2. **改写之后再上融合**：此时 BM25 有信号，混合检索/RRF 才有意义；同时评估 **Rerank（O1-R6）**；
  3. **拿 R3 当新的对照基线**（待补项 2 已关闭）：day19 的改写实验**一律与 R3 比**（同 k=8、同 qi=off、同 runs=3），**别再去和 runs=1 的 day16 R2 相减**；
  4. **O2-G2 模板 09 v2 + 消融**：打生成层的老靶子（**Q14/Q18 在三组里始终 FN 编造**，R3 里 FN = Q11/Q14/Q18）→ 加"**不得编造数字与引用**"硬约束，**把"检索层问题"和"生成层问题"分开报**（注意：**Q11 在 R3 里三次都编造、一致率 1.00**，是一个稳定的生成层靶子，比 Q18 更适合做消融的观察点）；
  5. **F 线推进**：F2 真实行业数据收集 / F3 真实 vs 合成对比微调（把"数据没意义"这条意见彻底闭环）；
  6. **收尾**：⚠ **唯一仍未关闭的是待补项 4 的后半**（`space_demo` 的 **DeepSeek 端到端**问答——要 key、花额度；**环境与 UI 冒烟已就绪**）与**5 的零散时间补记**；`171bca3` / `62a582b` 的 squash 若时间允许再做。**务必记得：`llm` 环境里不要再跑 `pip install -r`，要用 `conda activate demo`；conda 已换清华镜像（见教程「❗ 建环境连不上」）。**
  7. 零散：力扣 198 补做 + 统计八股串线② + 牛客真题。

---

> 汇报依据：`第四周\01-第四周详细计划.md`（D3 章节）、`第四周\01-第四周项目总结与优化路线图.md`、`第四周\day17\day17工作汇报.md`、day18 教程与全部产出（`data_selfcheck.py` / `retrieval_lab.py` / `retrieval_lab.json` / `build_paper_qa.py` / `sft_data_paper.json` / `app.py`）、`第四周\day17\result_lora_k8_runs3\`（R3 的 `run_log.txt` / `评测表.md` / `eval_results.json`）、`第四周\实验日志.md`、`第三周\day15\config.json`、`发布包\space_demo\requirements.txt`、当日实际运行记录（自检 **9/0/0/1** / 三通道 **4-4-3** / hyde **10-10-10** / 逐字回查 **90/90** / R3 **4-5-4-7 / F1 0.824 / 11-20** / `app.py --check` 通过 / `start.py --check` 指向 day18 / 指标表 `F1 0.706→0.842` / 静态核对体积 **13130 B**）与 git 提交记录（`171bca3` / `62a582b` / **`2660b33`** / **`6aa8cd7`** / **`27bd6e2`** / **`01f2d97`** / **`e703713`** / **`6ac1de9`** / 本次收尾一笔）整理。
>
> **数字纪律**：本报告所有数字均来自**实际运行产物**。**A/B/C 三条是"诊断数字"**（`data_selfcheck.py` / `retrieval_lab.py`），**只用于定位瓶颈，不作成绩引用、不进「一、实验记录表」**；**D 条（`build_paper_qa.py` 机器校验）与 F 条（R3，`eval_v2.py` 标准口径）是"可引用成绩"**。**HyDE 的 10/10 明确标注为"上界"**（人工理想查询），**绝不写成"提升到 100%"**；**R3 的 F1 低于 R1R2 时按 2 个格子如实解释，不写成"R3 最好"**。**未跑出来的一律不填、绝不预填**——今日**待补项 1/2/3/6/7 已在本次收尾中关闭，4 只有"静态核对"这一半完成、"真冒烟"仍如实挂账（不花额度），5 无可核验产物故不代填**；**负结果（RRF 融合变差）、自身笔误（F1 0.889→0.842）、自身事故（41 个包降级）均照实记录**，笔误已入 `实验日志.md` 的更正说明、同步 4 个文档并**独立成一笔提交 `6aa8cd7`**，事故已固化为 `requirements.txt` 的 `pydantic==2.10.6` 与教程的「❗ 装依赖必看」整节。

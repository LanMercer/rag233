# 大模型算法学习成果汇报（简历投递版）

> **汇报人**：985/211 数学（概率统计）硕士
> **学习周期**：2026/8/1 起（冲刺月）｜ **硬件环境**：NVIDIA RTX 3060 Laptop（6GB 显存）· CUDA 12.3 · PyTorch 2.5.1+cu121
> **学习主线**：环境与 Transformer 原理 → 本地模型部署 → 提示词工程 → 模型接口化 → RAG 全链路 → LoRA/QLoRA 参数高效微调 → 项目 MVP
> **覆盖范围**：Day1 ~ Day12 已学完并 git 存档；Day13 项目定型四件套已产出（教程 / 架构图 / 评测题 / 微调接口脚本），正在按《第三周\02-项目完结冲刺计划.md》收尾验收。
> **使用说明**：本汇报每一条知识都标注了**对应 Day**，可按"第三周/day12/day12-XXXX详细教程.md"这样的路径回溯原文复习。

---

## 一、三句话总结（自我介绍话术）

1. **数学背景直接变现**：用概率统计语言理解并讲解大模型全链路——softmax 输出 = 概率分布、注意力权重矩阵 = 转移概率矩阵、位置编码 = 频率指纹、温度 = 调熵、思维链 = 方差分解、检索 = 高维空间最近邻、LoRA = 低秩近似（SVD）。
2. **1 个月从零跑通"部署 → RAG → 微调"完整链路**：本地 4bit 部署 Qwen2.5-3B（实测显存 1.92GB）、跑通上传 PDF 即问即答的 RAG 问答系统（3 问全对 + 防幻觉验证）、QLoRA 微调只训 0.48% 参数产出风格化 adapter。
3. **一切结论都有亲手跑的代码与数字支撑**：手写多头注意力 vs 官方误差 5e-07、微调前后 5 题中 4 题肉眼可见差异、思维链 A/B 实验把数学正确率从 60% 拉到 84%。

---

## 二、学习成果总览（模块 → Day 映射表）

| 模块 | 主题 | 对应 Day | 核心产出 | 核心量化指标 |
|---|---|---|---|---|
| A | 大模型基础与 Transformer 原理 | Day1~5 | 环境就绪 + Transformer 结构图定稿 | 手写注意力误差 5e-07；seq2seq loss→0、测试 10/10 |
| B | 本地模型部署与推理 | Day6 | Qwen2.5-3B-Instruct 4bit 本地部署 | 显存实测 1.92GB（预算 3.0GB，省 1.1GB） |
| C | 提示词工程 | Day7~8 | 模板库 v2（20 个模板，5 大分类） | 思维链 A/B：正确率 60%→84%（N=5）、20%→50%（N=20） |
| D | 模型接口化与工程化 | Day9 | FastAPI OpenAI 兼容接口 | curl / requests / LangChain 三路调通 |
| E | RAG 检索增强生成 | Day10 | 上传 PDF 能问答的 demo | GMR 论文 3 问全对（带出处）；资料外问题防幻觉通过 |
| F | LoRA/QLoRA 参数高效微调 | Day11~12 | 300 条 SFT 数据 + 微调 adapter | 训练参数仅 0.4827%；微调前后 4/5 题肉眼可见差异 |
| G | 项目 MVP（完结冲刺中） | Day13~15 | 机器人领域垂直 RAG 问答系统 | 四层架构定型 + 20 条评测集 + 微调接口接入（冒烟测试待跑） |

---

## 三、知识体系详解（每条对应具体 Day）

### 模块 A｜大模型基础与 Transformer 原理（Day1~5）

#### A1. 深度学习环境与 GPU 工具链（Day1）
- 用 Miniconda 搭建 Python 3.11 + **GPU 版 PyTorch 2.5.1+cu121**，`torch.cuda.is_available()` = True；配齐 transformers / accelerate / datasets / langchain / chromadb / bitsandbytes 全依赖链。
- 理解模型权重格式（safetensors）、依赖版本兼容、显存/驱动约束（transformers 新版 torch 安全限制的绕行方案）。
- **能讲**：环境出问题的排查思路（conda 服务条款、驱动与 torch 版本匹配、控制台编码）。

#### A2. Transformer 整体架构认知（Day2）
- 精读 Jay Alammar《The Illustrated Transformer》：Encoder / Decoder / 自注意力 / 多头注意力 / 位置编码的整体结构与数据流向。
- **数学视角**：softmax 输出 = 概率分布；注意力 = 对 Value 的加权平均，权重由 Q、K 相似度经 softmax 决定；位置编码 = 给词编"座位号"。
- 产出整体结构图（`transformer_structure.png`）。

#### A3. 注意力机制与 Q/K/V（Day3）
- 看 3Blue1Brown 注意力可视化，理解注意力的动机、Query / Key / Value 三向量、softmax 权重。
- **亲手手算 3×3 注意力权重矩阵**（`softmax_attention.py`），确认每行和为 1 = **转移概率矩阵**。
- **数学加成实锤**：用**方差可加性**解释"为什么除以 √d"（d 个独立分量求和方差 = d，除以 √d 让方差回 1，防止 softmax 退化）。
- 词向量改造实验：给"猫""狗"加共享"动物"维度后，二者互相关注权重 **0.212 → 0.468**——词向量承载语义、注意力自动聚焦同类词。

#### A4. 多头注意力与位置编码实现（Day4）
- 手写 `nn.MultiheadAttention`：头 1 看身份（≈对角矩阵）、头 2 看动物类别（猫狗互相关注）；**手写 vs 官方最大误差 5e-07**——证明做的是同一件事。
- 位置编码实验：不加位置编码时"我爱你/你爱我"注意力输出完全一样，加了之后不同（**编码承载语序信息**）。
- 摸清 `torch.nn.Transformer` 源码与张量形状约定 `(seq, batch, dim)`。
- **第一次亲手训练出模型**：seq2seq 倒序 demo，**loss 1.856 → 0.000，测试 10/10 全对**（含 token 映射、标签掩码等工程细节）。

#### A5. Transformer 结构定稿：残差 / LayerNorm / FFN（Day5）
- 三件套公式与作用：残差 `x + 子层(x)`、LayerNorm `(x-均值)/σ×γ+β`、FFN `W2·ReLU(W1·x+b1)+b2`；标准"子层 → 残差加法 → LayerNorm"包装模式。
- 产出定稿结构图 `transformer_structure_full.png`（整体结构 + Encoder 单层解剖）。

#### A6. LoRA 数学原理（Day5）
- 纯 numpy 数学模拟（`lora_math_demo.py`）验证：① 预训练底座对特定任务误差 4.05；② **完整微调把旧任务冲掉 = 灾难性遗忘**（任务 A 误差 0→3.93）；③ ΔW 奇异值只有 3 个 → **低秩**；④ LoRA 用 `B·A`（秩 r）近似，r 是"改动大小"旋钮；⑤ 512×512 矩阵 r=16 近似误差 1.63e-15 几乎无损。
- **参数账**：全量 d²=12,845,056 vs LoRA 2dr=57,344 → **节省 99.55%**；Qwen2.5-7B LoRA 只训约 **0.084%** 参数。
- **显存账**：6G 显存选 Qwen2.5-3B-Instruct + 4bit ≈ 3.0GB（为 Day6 部署定方案）。

> **模块 A 可讲给面试官的一句话**：我能从零画出一张带残差、LayerNorm、FFN 的 Transformer 结构图，能手算注意力权重矩阵、能手写多头注意力并通过数值对拍，能用 SVD/低秩语言讲清 LoRA 为什么省钱省显存。

---

### 模块 B｜本地模型部署与推理（Day6）

#### B1. 开源模型 4bit 量化本地部署（Day6）
- 用 ModelScope 下载 **Qwen2.5-3B-Instruct**（约 6GB）；`BitsAndBytesConfig(load_in_4bit=True, quant_type="nf4", double_quant=True)` + `device_map="auto"` 加载，实现 **6G 显存跑 3B 模型**。
- 显存实测：`torch.cuda.memory_allocated()` = **1.92GB**（比 Day5 预算 3.0GB 还省 1.1GB，验证"会算账"）。
- 工程规范：模型权重不进 git（`.gitignore` 排除 + `git rm --cached` 教训）。

#### B2. 采样参数：温度与 top_p（Day6）
- **数学原理**：温度 T 作用在 logits 上，`p_i = exp(z_i/T)/Σ` —— 调熵；top_p 是"排序 → 累加 → 截断 → 重归一化"，切候选名单。
- 实测对比：T=0 / 0.7 / 1.2 与 top_p=0.5 / 0.9 / 1.0 三档交叉实验 + 熵对照表（T=0.5 熵≈0.44；T=2 熵≈1.02）。
- **能讲**：何时用低温（抽取/计算求稳）、何时用高温（创意发散）。

---

### 模块 C｜提示词工程（Day7~8）

#### C1. 三大核心概念（Day7）
- **few-shot（少样本）**：给几个输入-输出示例让模型照着回答；**思维链 CoT**：让模型把推理过程一步步写出来再给结论；**结构化提示词公式**：角色 + 任务 + 输入 + 输出格式 + 约束条件。
- **数学视角**：few-shot = 用示例做后验调整（把输出条件概率分布从"大而平"收成"小而尖"，贝叶斯先验→后验）；思维链 = 把高方差估计拆成多步低方差估计（方差分解 `Var(Y)=E[Var(Y|X)]+Var(E[Y|X])`）；提示词五件套 = 压缩输出分布的支持集。
- 产出《提示词模板库.md》v1（10 个模板，总结/翻译/改写/抽取/问答各 2，中英各半）。

#### C2. 进阶技巧与模板库成型（Day8）
- 角色扮演 = 给条件分布强先验；输出格式约束 = 把支持集压成 schema；迭代优化 = "写→跑→看→改"的手动梯度下降；抑制幻觉 = "分布尖 ≠ 分布准"。
- **模板迭代闭环实证**：模板 07（中文 JSON 抽取）v1（date 乱码）→ v2（person 抽错主体）→ v3（few-shot 换同构示例 + 字段级约束）→ **四字段全对**。
- 产出《提示词模板库.md》v2：**20 个模板、5 大分类**（通用助手/文本处理/抽取结构化/推理问答/代码），全部实测入库。
- **工程原则沉淀**："推理归模型、计算归代码"（模板 14 加权总分算错 = LLM 算术是模式匹配不是计算）。

#### C3. A/B 对比实验（统计学主场，Day7~8）
- 思维链把数学题正确率从 **60% 拉到 84%**（Day7，N=5）；Day8 复测 N=20 拉开 **2.5 倍**（20%→50%）。
- few-shot / 低温在"结构合规"任务上未拉开差距 —— **诚实汇报并分析原因**（结构是分布问题、数值/推理是能力问题）。
- 本地 Qwen 上完成 RAG 标准模板（模板 09：角色 + 资料带段落编号 [N] + 引用出处 [资料§N] + 防幻觉约束），为 Day10 RAG 直接复用。

---

### 模块 D｜模型接口化与工程化（Day9）

#### D1. OpenAI 兼容 API 与 FastAPI（Day9）
- 概念：OpenAI 兼容 API = 与 `/v1/chat/completions` 一致的接口，客户端可无缝切换供应商；FastAPI + uvicorn（ASGI）实现服务化。
- **逐字段默写返回 JSON**：`id / object / created / model / choices / message / role / content / finish_reason / usage`。
- 产出 `local_api.py`（239 行 → **619 行逐行注释版**，AST 校验一致）。

#### D2. 三种方式全调通（Day9）
- `curl.exe`（`chcp 65001` 防中文乱码）、Python `requests`、**LangChain `ChatOpenAI(base_url=本地端点)` 无缝切换**（三处切换点：base_url / api_key / model）。
- 讲清"为什么接口化"：前后端分离 / 统一调用 / 语言无关 / 为 RAG 打底。

---

### 模块 E｜RAG 检索增强生成（Day10）

#### E1. RAG 全链路六步（Day10）
- **文档加载**（PyPDF）→ **文本切块**（RecursiveCharacterTextSplitter，chunk 500 / overlap 50，中文标点分隔）→ **Embedding**（bge-small-zh-v1.5 中文向量模型本地化）→ **向量库**（Chroma，存向量 + 原文）→ **检索**（余弦相似度 + Top-K）→ **生成**（拼接上下文 + 模板 09 约束）。
- 工程坑位：langchain 1.x 大版本拆分（text-splitters 独立包）、Chroma 建库默认追加导致重复（建库前清空）、bge 模型 hf-mirror 连不上改 ModelScope 本地化。

#### E2. 数学视角贯通（Day10）
- 检索 = 高维向量空间里的**最近邻问题**；"打分 → 取 Top-K → 融合"与注意力三步走**同构**（Attention 是模型内部的软性检索，RAG 是模型外部的硬性筛选）。
- **手写简化版 RAG**（`manual_rag.py`）：`normalize_embeddings` 归一化 → `doc_vecs @ query_vec` 一行算余弦相似度 → `np.argsort(scores)[::-1][:k]` 取 Top-K，结果与 LangChain 版基本一致（原理实证）。

#### E3. 效果与防幻觉验证（Day10）
- 用自己的 GMR 机器人论文（9 页 → 105 chunk）问 **3 问全对**（原理/优点/缺点），答案带 [资料§N] 出处、检索片段与答案一一对应。
- **防幻觉验证通过**：问"资料中没有的缺点"，检索未召回相关段落时，模型如实答「资料中没有提到」，没有编造（模板 09 约束链路生效）。

---

### 模块 F｜LoRA/QLoRA 参数高效微调（Day11~12）

#### F1. 概念与数据准备（Day11）
- **三个新名词**：SFT（监督微调，用"问题-答案"配对数据训练，调整 `P(答案|问题)` 条件分布）；QLoRA（4bit 量化底座 + 低秩适配器，两个"省"叠加，6G 显存也能微调 3B）；PEFT（HuggingFace 高效微调工具箱）。三者关系：**用 PEFT 的工具，走 QLoRA 的方案，做 SFT 这件事**。
- **自制 300 条 SFT 数据**（`make_sft_data.py` + `sft_data.json`）：三字段（instruction / input / output）、**6 类任务**（公告抽取 61 / 研报问答 72 / 要点总结 57 / 数据计算 44 / 合规判断 44 / 概念解释 22）、**0 重复**（固定随机种子 + 去重逻辑）。题材主动定为**机器人行业**（20 家产业链公司语料，与论文领域对齐）。
- 数据质量检查：`datasets` 加载成功 / 条数落区间 / 字段齐全 / 空值 = 0 / 重复 = 0（五盏绿灯）。

#### F2. 训练原理五件套（Day12）
- **交叉熵 LM loss**：`-log p(真实答案)`，只对答案部分打分（题干标签 -100 掩码）——数学推导展开到"连乘变连加、凸函数惩罚不均匀"。
- **AdamW**：动量（EMA 方向惯性）+ 自适应步长（按梯度 RMS 标准化）+ 权重衰减（L2 正则）；学习率 2e-4（微调只在底座小幅调整，太大灾难性遗忘）。
- **epoch / 验证集 / 梯度累积**：90/10 留出法切分（eval_loss 防过拟合）；显存只装得下 2 条、攒 8 步再更新 = 等效 batch 16。

#### F3. QLoRA 微调实战（Day12）
- 配置：`BitsAndBytesConfig(4bit nf4)` + `LoraConfig(r=8, alpha=16, 挂 7 类线性层)` + `get_peft_model`。
- 训练：4bit 底座显存 1.92GB，3 epoch，train/eval loss 均下降；**trainable params 14,966,784 / 总 3.10B = 0.4827%**（LoRA 只训不到 0.5% 参数实锤）。
- **只存 adapter**（`adapter_config.json` + `adapter_model.safetensors`，约 20~30MB，无全量模型）；推理时合入权重 `W0 + B·A`。
- 版本兼容实战：transformers 5.x API 变更（`tokenizer` → `processing_class`、`warmup_ratio` → `warmup_steps`、`torch_dtype` → `dtype`），用 1 步冒烟测试验证链路。

#### F4. 微调效果评估（Day12）
- `compare_lora.py`：同一组 5 个测试问题（抽取/计算/概念/总结/合规），加载"原模型 vs 原模型 + adapter"对比。
- **结果：5 题中 4 题微调后明显更贴任务**（学会机器人行话 + 简洁专业口吻 + "先列式再代入"步骤）；**Q5 合规判断答错如实记录**并定位原因（训练数据里"合规。"开头样本带偏 → 格式学会 ≠ 内容学会），印证"RAG 管事实、微调管风格"的分工。

> **模块 F 可讲给面试官的一句话**：我从造 300 条领域 SFT 数据开始，到 QLoRA 只训 0.48% 参数跑通微调、只产出 20~30MB 的 adapter，并用"微调前后对比表"诚实量化了效果与失败案例。

---

## 四、项目成果（简历项目段落可直接引用）

### 项目名称：机器人行业垂直领域 RAG 问答系统

### 4.1 简历项目描述（现在就能投的版本，只用了已完成事实）

> 说明：本段全部使用已实测完成的事实与数字。评测数字来自 Day14 `eval.py` 首版评测表（Q9/Q18 人工复核改判后重跑确认版）；Day15 已于 2026-09-03 回填"效果量化"条目。复制时保留 markdown 格式。

```markdown
### 机器人行业垂直领域 RAG 问答系统（个人项目）

**技术栈**：Python / PyTorch / HuggingFace Transformers / PEFT / LangChain / Chroma / FastAPI / Qwen2.5-3B / bge-small-zh

- 构建"检索增强 + 领域微调"的垂直问答系统：上传领域 PDF 自动建库，问答时先检索资料片段，再由微调过的 Qwen 组织带出处回答。
- **RAG 全链路**：PDF 加载 → 中文切块 → bge-small-zh 向量化 → Chroma 存储 → 余弦相似度 Top-K 检索 → 提示词模板约束生成；用自备论文实测 3 问全对（答案带 [资料§N] 出处），资料外问题正确回答"资料中没有提到"（防幻觉验证通过）。
- **QLoRA 领域微调**：自制 300 条机器人行业三字段 SFT 数据（6 类任务、0 重复）；4bit 底座 + LoRA r=8 微调，训练参数仅占 0.48%，产出 20~30MB adapter；微调前后对比 5 题中 4 题输出显著更贴合领域任务。
- **接口化与工程化**：FastAPI 封装 OpenAI 兼容 /v1/chat/completions，curl / requests / LangChain 三种方式无缝调用；Qwen2.5-3B 4bit 实测显存 1.92GB（6G 显卡可本地跑通全链路）；问答端配置化（config.json 双 profile），支持一键切换"原模型 / 微调模型"。
- **效果量化（20 条评测题，资料内/资料外各 10，规则判分 + 人工复核）**：检索层命中率 20%、生成层正确率 20%、防幻觉正确率 60%、总正确率 40%；防幻觉按二分类算 F1=0.667（P=0.75 / R=0.60，混淆 TP=6 FP=2 FN=4 TN=8）。指标分层可归因到具体层——当前主瓶颈在检索召回（默认 TOP_K=4 下 8 条未召回），已列入完结后 P1 优化方向。
```
> 更新说明（2026-09-03）：原"接口化与工程化"一条补充了 MVP 一键切换；新增"效果量化"一条。P2 的"微调 vs 原模型提升对比"尚未跑，故本段不写"微调后提升 X%"这类字眼，避免虚报。

### 4.2 四层架构（面试讲架构用）

| 层 | 组件 | 负责什么 | 来源 |
|---|---|---|---|
| 检索层 | `rag_demo/`：PyPDF 加载 + 切块 + bge-small-zh 向量化 + Chroma 余弦相似度 Top-K | 从知识库找出相关段落（"答对"） | Day10 |
| 答案组织层 | 提示词模板库 v2 · 模板 09（资料带段落编号 + 引用出处 + 防幻觉约束） | 把资料拼成"只依据资料 + 带出处"的提示词 | Day8 |
| 风格/能力层 | QLoRA 微调 LoRA adapter（300 条机器人行业 SFT 数据，只训 0.48% 参数） | 让回答贴近行业专家口吻（"答得像"） | Day11~12 |
| 生成层 | Qwen2.5-3B-Instruct 4bit（显存 1.92GB）→ OpenAI 兼容 `/v1/chat/completions` | 把提示词变成通顺答案 | Day6 / Day9 |

- **数据流**：用户提问 → 检索层向量化 + Top-K 取段落 → 模板 09 拼上下文 → Qwen（挂 adapter，`W0 + B·A`）生成 → 带 [资料§N] 出处的答案。
- **分层价值**：每层独立调优、独立评估，出问题能定位到具体环节（答非所问查检索层、格式乱查模板、不像专家查 adapter、逻辑错查生成层）。
- **面试加分句**："RAG 管事实（答对）、微调管风格（答得像），两条路线互补，正好把三周所学串成一个完整项目。"

### 4.3 当前完成度盘点（Day15 项目完结节点）

| 状态 | 内容 |
|---|---|
| ✅ 已完成 | 四层架构定型 + 项目架构图（`项目架构图.png`）；20 条评测问题集（`eval_questions.json`）；复建库（GMR 约 105 chunk）；微调模型版接口（`local_api_lora.py`）+ curl 冒烟通过（记录已填）；Day14 `eval.py` 量化（首版评测表：检索 20% / 生成 20% / 防幻觉 60% / 总 40% / F1=0.667）；Day15 MVP 整合（`config.json` + `ask.py` 一键切换双链路，双链路冒烟通过）+ `README.md` + `效果评估报告.md` + 面试四问 Q1~Q4；git 已存档 |
| 🔄 完结后优化（P1/P2，不阻塞投递） | 检索调优（TOP_K 4→8 / chunk_size / Embedding 对比）；生成调优（temperature / 模板 09 迭代）；微调 vs 原模型"同一评测集"第二版对比；Rerank / 混合检索等进阶学习 |

### 4.4 可投版本 vs 完整版

| 版本 | 内容 | 何时可用 |
|---|---|---|
| **完整版（当前）** | 4.1 的项目描述（RAG + 微调 + 接口 + 效果量化四块硬结果，全部有实测数字） | **现在即可投**（Day15 已完结，2026-09-03） |
| 可选增强（P2） | 追加"微调 vs 原模型同一评测集"对比："20 条评测题，检索命中率 X%、生成正确率 X%、F1 X（微调后提升 X%）" | 第 4 周 P2 完成后再补，不阻塞投递 |

> 状态：Day14~15 已把 4.4 预留的"完整版"评测数字做出来并回填 4.1，"现在可投"与"完整版"已合并为同一版本。P2 的第二版对比（微调提升量化）留作可选增强，跑出来再加一行、勿虚报。

---

## 五、量化成果速查表（数字会说话）

| 指标 | 数值 | 对应 Day |
|---|---|---|
| 手写多头注意力 vs 官方实现误差 | 5e-07 | Day4 |
| 第一个 seq2seq 模型 | loss 1.856→0.000，测试 10/10 | Day4 |
| LoRA 参数节省 | 99.55%（全量 d² → 2dr） | Day5 |
| LoRA 低秩近似误差（512×512, r=16） | 1.63e-15 | Day5 |
| Qwen2.5-3B 4bit 显存（预算/实测） | 3.0GB / **1.92GB** | Day5/Day6 |
| 思维链 A/B 实验（数学正确率） | 60%→84%（N=5）；20%→50%（N=20） | Day7/Day8 |
| 提示词模板库 | 10 → 20 个（5 大分类，全实测） | Day7/Day8 |
| 模板迭代闭环 | 模板 07 v1→v3 四字段全对 | Day8 |
| RAG 问答 | 自备论文 3 问全对，带出处；防幻觉通过 | Day10 |
| 手写 RAG vs LangChain | 结果基本一致（原理实证） | Day10 |
| SFT 数据 | 300 条 / 三字段 / 6 类任务 / 0 重复 | Day11 |
| QLoRA 训练参数量 | 仅 **0.4827%**（14,966,784 / 3.10B） | Day12 |
| 微调产物体积 | adapter 仅 20~30MB | Day12 |
| 微调前后对比 | 5 题中 4 题肉眼可见差异（1 题如实记录失败） | Day12 |
| 项目评测（首版基线） | 20 题：检索命中 20% / 生成正确 20% / 防幻觉 60% / 总 40% / 防幻觉 F1=0.667 | Day14 |
| MVP 一键切换 | `config.json` 双 profile，原模型 / 微调模型链路双通（含防幻觉题都如实拒答） | Day15 |
| 力扣刷题 | 数组/哈希/双指针/字符串/链表/排序/递归/二分专题累计 20+ 题 | Day1~15 |

---

## 六、工程能力：踩坑与解决（面试"踩坑-解决"素材库，精选）

| 类别 | 问题 | 解决办法 | Day |
|---|---|---|---|
| 环境 | transformers 新版要求 torch≥2.6 与 cu121 驱动冲突 | 换用 safetensors 格式迷你模型绕开 | Day1 |
| 数值 | 手写多头注意力 vs 官方误差 1.87 | PyTorch 线性层是 `y = x·Wᵀ`，转置修复 → 5e-07 | Day4 |
| 数据 | 生成 SFT 数据检出 27 条完全重复 | 扩大语料池 + 生成循环去重 `if key in seen: continue` → 0 重复 | Day11 |
| 版本 | `Trainer` 报 `unexpected keyword argument 'tokenizer'` | 用 `inspect.signature` 查新签名，改名 `processing_class` | Day12 |
| 依赖 | langchain 1.x 切块模块报 ModuleNotFoundError | 改用独立包 `langchain-text-splitters` | Day10 |
| 部署 | `git add .` 误把 6GB 模型权重加入暂存区 | `.gitignore` 排除 + `git rm --cached`；模型权重永远不进 git | Day6 |
| 系统 | PowerShell 中文乱码 / GBK 编码错误 | `chcp 65001`、`python -X utf8`、脚本内置 UTF-8 输出加固 | Day2 起 |
| 工程 | 模板 07 中文 JSON 抽取 date 乱码、person 抽错 | 加约束 + 降温度 + 同构 few-shot 示例 + 字段级约束 → v3 全对 | Day8 |
| 工程 | LLM 加权总分算错 | 沉淀原则"推理归模型、计算归代码" | Day8 |

---

## 七、简历技能栈速查

- **框架与库**：PyTorch 2.5（GPU）、HuggingFace Transformers / PEFT / Datasets / Accelerate、bitsandbytes（4bit 量化）、LangChain、FastAPI、Chroma、Jupyter。
- **模型**：Qwen2.5-3B-Instruct（部署 + QLoRA 微调）、bge-small-zh-v1.5（中文 Embedding）。
- **技术点**：Transformer 原理、注意力机制、位置编码、残差/LayerNorm/FFN；量化部署（NF4）、采样参数（temperature/top_p）；提示词工程（few-shot/CoT/结构化）；RAG 全链路；LoRA/QLoRA/PEFT 微调；OpenAI 兼容接口服务化。
- **数学/统计**：概率分布与条件分布视角、低秩近似（SVD）、方差分解、贝叶斯先验/后验、熵、余弦相似度/最近邻、精确率/召回率/F1/AUC、交叉验证、A/B 实验设计。
- **其他**：Git 版本管理、Windows 环境工程问题排查、LeetCode 20+ 题（数组/哈希/双指针/字符串/链表/排序/递归）。

---

## 八、每日学习轨迹一页总览（Day1~Day13）

| Day | 日期（计划） | 主题 | 一句话成果 |
|---|---|---|---|
| Day1 | 8/1 | 环境搭建 | GPU 版 PyTorch 跑通，`torch.cuda.is_available()`=True |
| Day2 | 8/2 | Transformer 图解 | 整体结构图草稿 + 概率语言概念笔记 |
| Day3 | 8/3 | 注意力机制可视化 | 手算 3×3 注意力权重矩阵（转移概率矩阵）+ √d 方差解释 |
| Day4 | 8/4 | 多头注意力与位置编码 | 手写 vs 官方误差 5e-07；seq2seq 训练 10/10 |
| Day5 | 8/5 | 结构图定稿 + 微调认知 | 定稿结构图（残差/LN/FFN）+ LoRA 数学 demo + 显存方案 |
| Day6 | 8/8 | Qwen 本地部署 | 4bit 部署跑通，显存实测 1.92GB，temperature/top_p 对比 |
| Day7 | 8/9 | 提示词工程入门 | few-shot/CoT/结构化模板 + 思维链 A/B（60%→84%） |
| Day8 | 8/10 | 提示词进阶 + 模板库 v2 | 20 模板 5 分类 + 模板 07 迭代闭环四字段全对 |
| Day9 | 8/11 | 模型接口化 | FastAPI OpenAI 兼容接口，curl/requests/LangChain 三路调通 |
| Day10 | 8/12 | RAG 全链路 | 上传 PDF 能问答 demo，GMR 论文 3 问全对 + 防幻觉通过 |
| Day11 | 8/15 | 微调（一）数据与环境 | 300 条机器人行业 SFT 数据（0 重复）+ QLoRA 环境五件套全绿 |
| Day12 | 8/16 | 微调（二）跑通训练 | QLoRA 只训 0.48% 参数，产出 20~30MB adapter，对比表 4/5 有效 |
| Day13 | 8/17 | 项目定型 + 架构定型 | 四层架构图 + 20 条评测题 + 微调接口脚本（curl 冒烟测试待跑） |

---

## 附：当前状态与下一步

- **已完成**：Day1~12 全部主线学习并 git 存档（"部署 → Prompt 工程 → 接口化 → RAG → 微调"全链路闭环）。
- **进行中（Day13~15，按《第三周\02-项目完结冲刺计划.md》）**：项目定型四件套已产出（架构图 / 20 条评测题 / 微调接口脚本 / 冒烟测试记录模板），剩余 = 复建库 + curl 冒烟测试 + `eval.py` 量化（精确率/召回率/F1）+ MVP 整合 + README + 效果评估报告。**项目完结优先，调优与补充学习放在完结之后。**
- **投简历**：第四节 4.1 的项目描述现在就能投（全部是已实测事实）；补齐 Day14~15 评测数字后升级为完整版。
- **建议配套复习路径**：按本汇报第二/三节的"对应 Day"定位到 `第X周/dayY/` 文件夹，配合详细教程 + 脚本 + 工作汇报三件套回顾；面试前重点演练"三句话总结"与"四张账"（数据账 / 参数账 / 显存账 / 训练账 + 项目账）。

---

> 汇报依据：《00-基础内容总纲》《第一周/week1工作汇报》《第二周/week2工作汇报》《第三周/day11、day12 工作汇报》及各日详细教程、脚本实测运行结果、git 提交记录整理。未完成项（Day13 及以后）如实标注，绝不虚报。

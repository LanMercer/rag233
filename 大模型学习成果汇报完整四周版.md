# 大模型学习成果汇报 · 完整四周版（简历投递用）

> **汇报人**：985/211 数学（概率统计）硕士
> **学习周期**：2026/8/1 起（8 月冲刺月 → 9 月秋招黄金月，边投边学）
> **硬件环境**：NVIDIA RTX 3060 Laptop（6GB 显存）· CUDA 12.3 · GPU 版 PyTorch 2.5.1+cu121
> **学习主线**：环境与 Transformer 原理 → 本地模型部署 → 提示词工程 → 模型接口化 → RAG 全链路 → LoRA/QLoRA 微调 → 项目定型与量化评测 → 项目整改（优化工程 + 在线交付 + 真实数据）
> **覆盖范围**：**Day1 ~ Day20 全部完成**（四周，每周 5 个工作日），主线全部 git 存档
> **使用说明**：本汇报每一段知识都标注了**对应 Day**，可按 `第X周/dayY/` 文件夹回溯"详细教程 + 脚本 + 工作汇报"三件套复习。

---

## 一、三句话总结（自我介绍话术）

1. **数学背景直接变现为差异化武器**：用概率统计语言理解并讲解大模型全链路——softmax 输出 = 概率分布、注意力权重矩阵 = 转移概率矩阵、位置编码 = 频率指纹、温度 = 调熵、思维链 = 方差分解、检索 = 高维空间最近邻、RAG 评测 = Recall@K、防幻觉 F1 = 混淆矩阵口径、LoRA = 低秩近似（SVD）、微调 = 调整条件概率分布。
2. **一个月从零跑通"部署 → RAG → 微调 → 优化 → 交付"完整链路**：6G 显存本地 4bit 部署 Qwen2.5-3B（实测 1.92GB）→ 跑通上传 PDF 即问即答的 RAG 系统 → QLoRA 只训 0.48% 参数微调出领域 adapter → 用同一把评测尺子把检索命中率从 20% 提到 **40%**、防幻觉 F1 从 0.667 提到 **0.824** → 项目公网在线可访问。
3. **一切结论都有亲手跑的代码与数字支撑，且负结果照实记录**：手写多头注意力 vs 官方误差 5e-07；微调前后 5 题 4 题有效；17 轮对比实验 + 12 行逐步骤贡献 + **8 条失败尝试记录**，其中 **2 轮逆预期结果被写成主结论**（原模型 F1 0.947 高于微调 0.824）——**我报的是跑出来的数，不是想要的数**。

---

## 二、学习成果总览（模块 → Day 映射表）

| 模块 | 主题 | 对应 Day | 核心产出 | 核心量化指标 |
|---|---|---|---|---|
| **A** | 大模型基础与 Transformer 原理 | **Day1~5** | 环境就绪 + Transformer 结构图定稿 | 手写注意力误差 5e-07；seq2seq loss→0、测试 10/10 |
| **B** | 本地模型部署与推理 | **Day6** | Qwen2.5-3B-Instruct 4bit 本地部署 | 显存实测 1.92GB（预算 3.0GB，省 1.1GB） |
| **C** | 提示词工程 | **Day7~8** | 模板库 v2（20 个模板，5 大分类） | 思维链 A/B：60%→84%（N=5）、20%→50%（N=20） |
| **D** | 模型接口化与工程化 | **Day9** | FastAPI OpenAI 兼容接口 | curl / requests / LangChain 三路调通 |
| **E** | RAG 检索增强生成 | **Day10** | 上传 PDF 能问答的 demo | 自备论文 3 问全对（带出处）；防幻觉通过 |
| **F** | LoRA/QLoRA 参数高效微调 | **Day11~12** | 300 条 SFT 数据 + 微调 adapter | 只训 0.4827% 参数；adapter 仅 20~30MB |
| **G** | 项目定型、量化评测与完结 | **Day13~15** | 四层架构 + 20 条评测集 + 一键切换 MVP | 首版基线：检索 20% / 生成 20% / 防幻觉 F1 0.667 |
| **H** | 项目整改：优化工程 + 在线交付 + 真实数据 | **Day16~20** | 优化成果报告 + Web UI + 在线 Demo + 201 条真实数据 | 检索 20%→**40%**、F1 0.667→**0.824**、总 40%→**55%** |
| **I** | 副线：力扣刷题 + 统计建模 | **Day1~20 零散时间** | 40+ 题 + 6 大统计主题 + 牛客真题 | 数组→哈希→双指针→字符串→链表→排序→递归→二分→DP |

---

## 三、四周全景地图（Day1~Day20 一览）

| 周次 | Day 范围 | 主题 | 周目标 | 达成 |
|---|---|---|---|---|
| 第 1 周 | **Day1~5** | 环境 + Transformer 认知 + Python 补缺 | 本地能跑 + 能画 Transformer 结构图 | ✅ 结构图定稿 + 首个 seq2seq 训练成功 |
| 第 2 周 | **Day6~10** | Qwen 部署收尾 + Prompt 工程 + RAG 全链路 | 上传 PDF 能问答的 demo | ✅ GMR 论文 3 问全对 |
| 第 3 周 | **Day11~15** | LoRA 微调 + 项目定型开发（攻坚周）→ 项目完结冲刺 | 微调跑通 + 项目 MVP | ✅ 完结验收 5 条全绿 |
| 第 4 周 | **Day16~20** | 项目整改：优化成果 + 可交付 + 真实数据 | 有提升、能用、数据真实、能复述 | ✅ 优化成果报告 + 在线 Demo + 201 条真实数据 |

---

## 四、知识体系详解（每条对应具体 Day）

### 模块 A｜大模型基础与 Transformer 原理（Day1~5）

#### A1. 深度学习环境与 GPU 工具链（Day1）
- Miniconda 搭建 Python 3.11 + **GPU 版 PyTorch 2.5.1+cu121**，`torch.cuda.is_available()` = True；配齐 transformers / accelerate / datasets / langchain / chromadb / bitsandbytes 全依赖链。
- 理解模型权重格式（safetensors）、依赖版本兼容、显存与驱动约束（新版 transformers 的 torch 安全限制绕行方案）。
- **能讲**：环境问题排查思路（conda 服务条款、驱动与 torch 版本匹配、控制台编码、`pip install -r` 改环境的风险）。

#### A2. Transformer 整体架构认知（Day2）
- 精读 Jay Alammar《The Illustrated Transformer》：Encoder / Decoder / 自注意力 / 多头注意力 / 位置编码的整体结构与数据流向。
- **数学视角**：softmax 输出 = 概率分布；注意力 = 对 Value 的加权平均，权重由 Q、K 相似度经 softmax 决定；位置编码 = 给词编"座位号"。
- 产出整体结构图 `transformer_structure.png`。

#### A3. 注意力机制与 Q/K/V（Day3）
- 看 3Blue1Brown 注意力可视化，理解注意力动机、Query / Key / Value 三向量、softmax 权重。
- **亲手手算 3×3 注意力权重矩阵**（`softmax_attention.py` / `.ipynb`），确认每行和为 1 = **转移概率矩阵**。
- **数学加成实锤**：用**方差可加性**解释"为什么除以 √d"（d 个独立分量求和方差 = d，除以 √d 让方差回到 1，防止 softmax 退化）。
- 词向量改造实验：给"猫""狗"加共享"动物"维度后，二者互相关注权重 **0.212 → 0.468**——词向量承载语义、注意力自动聚焦同类词。

#### A4. 多头注意力与位置编码实现（Day4）
- 手写多头注意力：头 1 看身份（≈对角矩阵）、头 2 看动物类别（猫狗互相关注）；**手写 vs 官方 `nn.MultiheadAttention` 最大误差 5e-07**——证明做的是同一件事。
- 位置编码实验：不加位置编码时"我爱你/你爱我"注意力输出完全一样，加了之后不同（**编码承载语序信息**）；奇数维 sin/cos 列数对不上需裁剪。
- 摸清 `torch.nn.Transformer` 源码与张量形状约定 `(序列长度, 批量, 维度)`，Encoder/Decoder 只改内容不改形状。
- **第一次亲手训练出模型**：seq2seq 倒序 demo，**loss 1.856 → 0.000，测试 10/10 全对**（含 token 词表映射、标签掩码等工程细节）。

#### A5. Transformer 结构定稿：残差 / LayerNorm / FFN（Day5）
- 三件套公式与作用：残差 `x + 子层(x)`、LayerNorm `(x-均值)/σ×γ+β`、FFN `W2·ReLU(W1·x+b1)+b2`；标准"子层 → 残差加法 → LayerNorm"包装模式。
- 产出定稿结构图 `transformer_structure_full.png`（上半整体结构 + 下半 Encoder 单层解剖，标注红色 ⊕ 残差跳线、LayerNorm、FFN）。

#### A6. LoRA 数学原理（Day5）
- 纯 numpy 数学模拟（`lora_math_demo.py`）验证：① 预训练底座对特定任务误差 4.05；② **完整微调把旧任务冲掉 = 灾难性遗忘**（任务 A 误差 0→3.93）；③ ΔW 奇异值只有 3 个 → **低秩**；④ LoRA 用 `B·A`（秩 r）近似，r 是"改动大小"旋钮；⑤ 512×512 矩阵 r=16 近似误差 **1.63e-15** 几乎无损。
- **参数账**：全量 d²=12,845,056 vs LoRA 2dr=57,344 → **节省 99.55%**；Qwen2.5-7B LoRA 只训约 **0.084%** 参数。
- **显存账**：6G 显存选 Qwen2.5-3B-Instruct + 4bit ≈ 3.0GB（为 Day6 部署定方案）。

> **模块 A 可讲给面试官的一句话**：我能从零画出一张带残差、LayerNorm、FFN 的 Transformer 结构图，能手算注意力权重矩阵、能手写多头注意力并通过数值对拍，能用 SVD/低秩语言讲清 LoRA 为什么省参数省显存。

---

### 模块 B｜本地模型部署与推理（Day6）

#### B1. 开源模型 4bit 量化本地部署（Day6）
- 用 ModelScope（阿里魔搭，国内下载快）下载 **Qwen2.5-3B-Instruct**（13 个文件约 6GB）。
- `BitsAndBytesConfig(load_in_4bit=True, quant_type="nf4", double_quant=True)` + `device_map="auto"` 加载，加载耗时 3~6 秒，**6G 显存跑起 3B 模型**。
- 显存实测：`torch.cuda.memory_allocated()` = **1.92GB**（比 Day5 预算 3.0GB 还省 1.1GB）。
- 工程规范：模型权重永不进 git（`.gitignore` 排除 `download/` + `git rm --cached` 教训）。

#### B2. 采样参数：温度与 top_p（Day6）
- **数学原理**：温度 T 作用在 logits 上，`p_i = exp(z_i/T)/Σ` —— **调熵**；top_p 是"排序 → 累加 → 截断 → 重归一化"，切候选名单。
- 实测对比：T=0 / 0.7 / 1.2 与 top_p=0.5 / 0.9 / 1.0 交叉实验 + 熵对照表（T=0.5 熵≈0.44；T=1 熵≈1.02）。
- **能讲**：何时用低温（抽取/计算求稳）、何时用高温（创意发散）；温度不改变知识、只改变表达确定性。

---

### 模块 C｜提示词工程（Day7~8）

#### C1. 三大核心概念（Day7）
- **few-shot（少样本）**：给几个输入-输出示例让模型照着回答；**思维链 CoT**：让模型把推理过程一步步写出来再给结论；**结构化提示词公式**：角色 + 任务 + 输入 + 输出格式 + 约束条件。
- **数学视角**：few-shot = 用示例做后验调整（把输出条件概率分布从"大而平"收成"小而尖"，贝叶斯先验→后验）；思维链 = 把高方差估计拆成多步低方差估计（方差分解 `Var(Y)=E[Var(Y|X)]+Var(E[Y|X])`，数字直觉 0.95³≈0.857）；提示词五件套 = 压缩输出分布的支持集。
- 产出《提示词模板库.md》v1（10 个模板，总结/翻译/改写/抽取/问答各 2，中英各半）。

#### C2. 进阶技巧与模板库成型（Day8）
- 角色扮演 = 给条件分布强先验；输出格式约束 = 把支持集压成 schema（条件熵断崖下降）；迭代优化 = "写→跑→看→改"的手动梯度下降；抑制幻觉 = "分布尖 ≠ 分布准"。
- **模板迭代闭环实证**：模板 07（中文 JSON 抽取）v1（date 乱码）→ v2（person 抽错主体"李芳"、amount 抽空）→ v3（few-shot 换同构双人示例 + 字段级约束写死）→ **实测四字段全对**。
- 产出《提示词模板库.md》v2：**20 个模板、5 大分类**（通用助手/文本处理/抽取结构化/推理问答/代码），全部实测入库。
- **模板 09 升级为 RAG 标准版** ⭐：角色 + 资料带段落编号 [N] + 引用出处 [资料§N] + 防幻觉约束 —— 直接成为 Day10 与整个项目的"答案组织层"。
- **工程原则沉淀**："**推理归模型、计算归代码**"（模板 14 加权总分算错 = LLM 算术是模式匹配不是计算）。

#### C3. A/B 对比实验（统计学主场，Day7~8）
- 思维链把数学题正确率从 **60% 拉到 84%**（Day7，N=5）；Day8 复测 N=20 拉开 **2.5 倍**（20%→50%）。
- few-shot / 低温在"结构合规"任务上未拉开差距 —— **诚实汇报并分析原因**（结构是分布问题、数值/推理是能力问题）。

---

### 模块 D｜模型接口化与工程化（Day9）

#### D1. OpenAI 兼容 API 与 FastAPI（Day9）
- 概念：OpenAI 兼容 API = 与 `/v1/chat/completions` 格式一致的接口，客户端代码可无缝切换供应商；FastAPI + uvicorn（ASGI）实现服务化。
- **逐字段默写返回 JSON**：`id / object / created / model / choices / message / role / content / finish_reason / usage`。
- 产出 `local_api.py`（239 行 → **619 行逐行注释版**，AST 结构校验一致）。

#### D2. 三种调用方式全调通（Day9）
- `curl.exe`（`chcp 65001` 防中文乱码）、Python `requests`（健康检查/普通对话/RAG 资料问答/温度对比）、**LangChain `ChatOpenAI(base_url=本地端点)` 无缝切换**（三处切换点：base_url / api_key / model）。
- 讲清"为什么接口化"四点：前后端分离 / 统一调用 / 语言无关 / 为 RAG 打底。
- 概念补充：ChatML（对话标记格式）、HTTP/JSON/ASGI 基础。

---

### 模块 E｜RAG 检索增强生成（Day10）

#### E1. RAG 全链路六步（Day10）
- **文档加载**（PyPDF，GMR 论文 9 页）→ **文本切块**（`RecursiveCharacterTextSplitter`，chunk 500 / overlap 50，中文标点分隔）→ **Embedding**（`bge-small-zh-v1.5` 中文向量模型，ModelScope 本地化免联网）→ **向量库**（Chroma，存向量 + 原文）→ **检索**（余弦相似度 + Top-K）→ **生成**（拼接上下文 + 模板 09 约束）。
- 讲清"为什么 RAG 不用重新训练"：成本低 / 更新快 / 可解释 / 权限可控。
- 工程坑位：langchain 1.x 大版本拆分（`langchain-text-splitters` 独立包）、Chroma `from_documents` 默认追加导致重复（建库前清空）。

#### E2. 数学视角贯通（Day10）
- 检索 = 高维向量空间里的**最近邻问题**；"打分 → 取 Top-K → 融合"与注意力三步走**同构**（Attention 是模型内部的软性检索，RAG 是模型外部的硬性筛选）。
- **手写简化版 RAG**（`manual_rag.py`）：`normalize_embeddings` 归一化 → `doc_vecs @ query_vec` 一行算余弦相似度 → `np.argsort(scores)[::-1][:k]` 取 Top-K，结果与 LangChain 版基本一致（**原理实证，非调包**）。

#### E3. 效果与防幻觉验证（Day10）
- 用自己的 GMR 机器人论文（105 chunk）问 **3 问全对**（原理/优点/缺点），答案带 [资料§N] 出处、Top-K 检索片段与答案一一对应。
- **防幻觉验证通过**：问"资料中没有的缺点"，检索未召回相关段落时，模型如实答「资料中没有提到」，没有编造。
- 产出 `rag_结构图.png`（建库①~④ + 问答⑤⑥）。

---

### 模块 F｜LoRA/QLoRA 参数高效微调（Day11~12）

#### F1. 概念与数据准备（Day11）
- **三个新名词**：**SFT**（监督微调，用"问题-答案"配对数据再训一遍，调整 `P(答案|问题)` 条件分布，从"大而平"收成"小而尖"）；**QLoRA**（4bit 量化底座 + 低秩适配器，两个"省"叠加，6G 显存也能微调 3B）；**PEFT**（HuggingFace 高效微调工具箱）。三者关系：**用 PEFT 的工具、走 QLoRA 的方案、做 SFT 这件事**。
- **本周心法**：RAG 管"事实"（答对）、微调管"风格/能力"（答得像）——"RAG 是考试带资料翻书，微调是平时把套路练进肌肉记忆"。
- **自制 300 条 SFT 数据**（`make_sft_data.py` + `sft_data.json`）：三字段（instruction / input / output）、**6 类任务**（公告抽取 61 / 研报问答 72 / 要点总结 57 / 数据计算 44 / 合规判断 44 / 概念解释 22）、**0 重复**（固定随机种子 + 去重逻辑）。题材主动定为**机器人行业**（20 家产业链公司语料），与 Day10 论文领域对齐。
- 数据质量五查：`datasets` 加载成功 / 条数落在 200~500 区间 / 字段齐全 / 空值 = 0 / 重复 = 0（五盏绿灯）。
- 环境检查：transformers 5.14.1 / peft 0.20.0 / bitsandbytes 0.50.0 / accelerate 1.14.0 / datasets 5.0.1 全绿，CUDA True，可用显存 5.0GB。

#### F2. 训练原理五件套（Day12）
- **交叉熵 LM loss**：模型对词表输出概率分布，损失 = 标准答案位置概率的负对数 `-log p(真实答案)`，**只对答案部分打分**（题干标签 -100 掩码）——推导到"连乘变连加、凸函数惩罚不均匀"。
- **AdamW**：动量（EMA 方向惯性）+ 自适应步长（按梯度 RMS 标准化）+ 权重衰减（L2 正则防过拟合）。
- **学习率 2e-4**：微调只在底座上小幅调整，太大灾难性遗忘。
- **epoch / 验证集 / 梯度累积**：90/10 留出法切分（eval_loss 防过拟合）；显存只装得下 2 条、攒 8 步再更新 = **等效 batch 16**。
- 双版讲解（技术版 + "摸黑下山"/"只考 300 道题的考试"通俗版）都写进了教程。

#### F3. QLoRA 微调实战（Day12）
- 配置：`BitsAndBytesConfig(4bit nf4)` + `LoraConfig(r=8, alpha=16, 挂 7 类线性层)` + `get_peft_model`。
- 训练：4bit 底座显存 1.92GB，3 epoch，train/eval loss 均下降；**trainable params 14,966,784 / 总 3.10B = 0.4827%**（LoRA 只训不到 0.5% 参数实锤）。
- **只存 adapter**（`adapter_config.json` + `adapter_model.safetensors`，约 20~30MB，无全量模型）；推理时合入权重 `W0 + B·A`。
- `train_lora.py` 428 行逐行中文注释（应学习反馈把讲解写进代码文件本身）。
- 版本兼容实战：transformers 5.x API 三连变更（`tokenizer` → **`processing_class`**、`warmup_ratio` → **`warmup_steps`**、`torch_dtype` → **`dtype`**），用 1 步冒烟测试（SMOKE_OK，loss 0.87）验证链路再正式训练。

#### F4. 微调效果评估（Day12）
- `compare_lora.py`：同一组 5 个测试问题（抽取/计算/概念/总结/合规），加载"原模型 vs 原模型 + adapter"对比。
- **结果：5 题中 4 题微调后明显更贴任务**（学会机器人行话 + 简洁专业口吻 + "先列式再代入"步骤）；**Q5 合规判断答错如实记录**并定位原因（训练数据里"合规。"开头样本带偏 → **格式学会 ≠ 内容学会**）。
- **四张账闭环**：数据账（300 条 / 三字段 / 6 类 / 0 重复）、参数账（0.4827%）、显存账（4bit 底座 1.92GB，训练峰值 <6G）、训练账（3 epoch / 等效 batch 16 / adapter 仅 20~30MB）。

> **模块 F 可讲给面试官的一句话**：我从造 300 条领域 SFT 数据开始，到 QLoRA 只训 0.48% 参数跑通微调、只产出 20~30MB 的 adapter，并用"微调前后对比表"诚实量化了效果与失败案例。

---

### 模块 G｜项目定型、量化评测与完结（Day13~15）

#### G1. 项目定性与四层架构（Day13）
- **项目一句话**：机器人领域 RAG 文档问答系统——上传领域 PDF 自动建库，问答先检索资料片段，再由 QLoRA 微调过的 Qwen2.5-3B 组织**带 [资料§N] 出处**的回答。
- **MVP"不做"清单**（体现取舍意识）：不做多轮对话 / 并发 / 前端花活 / 流式输出 / 全量微调 / 联网搜索——**小而完整**。
- **四层架构**（产出 `项目架构图.png`）：

| 层 | 组件 | 负责什么 | 来源 |
|---|---|---|---|
| 检索层 | Chroma 向量库 + bge-small-zh + 余弦 Top-K | 找出相关段落（"答对"） | Day10 |
| 答案组织层 | 模板库 v2 · 模板 09 | 拼"只依据资料 + 带出处"的提示词 | Day8 |
| 风格/能力层 | QLoRA LoRA adapter | 让回答像行业专家（"答得像"） | Day12 |
| 生成层 | Qwen2.5-3B-Instruct 4bit → OpenAI 兼容接口 | 把提示词变成答案 | Day6/9 |

- **数学视角（面试可讲）**：`P(答案|问题) ≈ P_生成(答案 | 模板09(检索(问题)))` —— **分层 = 条件概率分解**；每层独立调优、独立评估，出问题能定位到具体环节（答非所问查检索层、格式乱查模板、不像专家查 adapter、逻辑错查生成层）。

#### G2. 评测体系设计（Day13~14）
- **20 条固定评测题**（`eval_questions.json`）：**资料内有答案 10 条**（带 `expected_keywords` + `source_chunk`）+ **资料外/反事实 10 条**（应如实拒答）——**先定卷子再谈分数**，这是统计建模的基本纪律。
- **`eval.py` 三组指标 + F1**（Day14）：① **检索层命中率**（`source_chunk` 是否被 Top-K 召回 = Recall@K）；② **生成层正确率**（资料内关键词命中）；③ **防幻觉正确率**（资料外如实拒答）；外加**防幻觉 F1**（把 20 题当"该不该拒答"二分类，混淆矩阵口径）。
- **诚实性设计**：规则判分 + `MANUAL_FIXES` 人工复核改判（Q9/Q18）——**宁可数字变低，也不让假阳性进简历**。

#### G3. 微调模型接入接口（Day13）
- `local_api.py` → `local_api_lora.py`：加 `ADAPTER_PATH` + 模型名 `Qwen2.5-3B-Instruct-LoRA` + lifespan 里 `PeftModel.from_pretrained` 挂 adapter。
- **接口逻辑一行未改**（路由 / ChatML 拼装 / 返回格式全复用）——实证"微调后的模型换模型路径即可接入"。
- curl 三接口全通（/health → /v1/models → /v1/chat/completions）；接口层风格差异可见（同题原模型 143 token 百科式 vs 微调模型 21 token 一句话定义式）。

#### G4. MVP 整合与项目完结（Day15）
- **一键切换**：`config.json`（`default_profile` + `demo_questions` + `persist_dir` + `embed_model` + original/lora 两个 profile）+ `ask.py`（`load_config` / `resolve_path` / **服务身份检查 `check_server`**）——把 Day14"服务在跑 ≠ 服务正确"的坑**代码化**。
- **双链路冒烟**：链路 A（微调模型）与链路 B（原模型）各 3 条 demo 题全通，两路检索 Top-K 完全一致（同库同参），差异纯在生成层；资料外题两条链路都如实拒答。
- **项目完结验收 5 条全 ✅**：能演示（一键切换双链路）/ 能量化（20 题三组指标 + F1）/ 有文档（README）/ 有报告（效果评估报告）/ 已存档（commit `3001cc4`）。
- 首版基线数字（Day14，**这就是第四周优化的起点**）：**检索 2/10 = 20%、生成 2/10 = 20%、防幻觉 6/10 = 60%、总 8/20 = 40%、防幻觉 F1 = 0.667**（TP6 / FP2 / FN4 / TN8）。

---

### 模块 H｜项目整改：优化工程 + 可交付 + 真实数据 + 叙事（Day16~20）

> **为什么要整改**（HR 反馈三条）：① 项目像"流程"，要有**能用**的成果或**优化前后提升**的成果；② 要打通、能复述、讲出个性化；③ 300 条 SFT 数据若是随机生成的没意义，要**论文相关或真实数据**。
> 本周**不学新知识点**，主线全部是"把已有的东西做出成果"——用**同一把评测尺子**跑出"优化前 → 优化后"的数字。

#### H1. 尺子校准：把"一次性脚本"升级成"可配置评测尺子"（Day16）
- `eval_v2.py`（737 行，**六个核心旋钮**）：`--profile`（原模型/微调模型）、`--top-k`、`--temperature`、`--runs`（多跑取多数投票）、`--template`、`--out`（一轮实验一个目录、互不覆盖）。
- **三处机制升级**：
  - ① **双轨检索判据**：`strict`（Top-K ∩ `source_chunk`，沿用旧口径偏严）+ `loose`（期望关键词是否落在 Top-K 上下文，治"没召回该段却答对了"的反例）→ 两列并列报、口径透明；
  - ② **多数投票 + 一致率**：把"随机抖动"和"真实提升"分开；
  - ③ **`--dry-run` 冒烟** + **Tee 双写日志**（绕开 PowerShell 中文路径重定向乱码）。
- **B0 基线复跑的关键发现**：无改动下也有**运行间波动**（生成 1/10 vs day14 的 2/10、F1 0.750 vs 0.667）→ **直接决定"对外引用的定稿数字必须 `--runs 3` 投票 + 报一致率（≥0.93 才算稳）"**。
- **R2：TOP_K 4→8**（第一个"优化后"数字）：检索 20%→**40%**、生成 10%→**40%**、防幻觉 60%→70%、F1 0.750→**0.824**、总 35%→**55%**。
- 建 `第四周\实验日志.md`（实验记录表 + 决策门 + 失败尝试记录三节）。

#### H2. 第一轮真正的受控实验：官方推荐做法反而变差（Day17）
- **尺子最后两处刻度**：① `KEYWORD_PATCH`（Q5 关键词 `LAFAN1` 放宽，用覆盖而非改原 json + 加开关）；② `--query-instruction`（先实测本机 `HuggingFaceEmbeddings` 不支持 `query_instruction` → 走**薄封装只重写 `embed_query`、段落侧一字不动、不用重建向量库**）。
- **B0r3 校准后可引用基线**：k=4 / runs=3 → 检索 2/10、生成 2/10、防幻觉 6/10、F1 0.706、总 40%、**一致率 0.950**。
- **R1：bge instruction 前缀 —— 负结果**（假设①被证伪）：检索 strict **20% → 0%（全灭）**、F1 0.706→0.625。
  - **机制追查（今天最值钱的"为什么"）**：前缀没让向量"变差"，而是把期望段**整体下压 1 名**（chunk 3 从第 4 名掉到第 5 名，k=4 时第 5 名看不到）→ **"排名位移"而非"语义退化"**；k=8 时位移被窗口掩盖 → 解释了交互效应。
  - **教训**：`HuggingFaceEmbeddings` 的 instruction 是"官方推荐"而非"一定有效"，**必须用自己的固定评测集验证**。
- **R1R2**（前缀 + k=8）：总 35%→**60%**、F1 0.842，但两变量不可加（有交互）→ 决策门选择**关前缀、保留 k=8**。
- 一键启动 `start.py` / `start.bat`（体检四项：文件 / 依赖 / 端口 / **服务身份**）。

#### H3. 把瓶颈从"检索参数"钉到"查询侧语言错位"（Day18）
- **数据自检 `data_selfcheck.py`**：三重核对（chunk 是否存在 / 关键词能否在标注段落内字面命中 / 是否只在别处）→ **9/10 标注自洽、0 错位、0 chunk 缺失** → **一句话结论：标注可信，检索未命中属真实失败，瓶颈在检索侧不在标注侧**（排除"题目错了"这个假设）。
- **`retrieval_lab.py`（把检索层单独拎出来做便宜实验）** ⭐ 今天最关键的诊断：

| 查询模式 | vector strict | bm25 strict | hybrid strict | 命中题并集 |
|---|---|---|---|---|
| **plain**（中文问句，现状） | 4/10 | 4/10（中文题大面积 `top=[]`） | 3/10 | **v∪b = 7/10** |
| **hyde**（英文术语理想查询，**上界**） | 10/10 | 10/10 | 10/10 | —— |

  → **同一套库、同一套判据，只把查询从中文字面换成英文术语，strict 从 40% 直接到 100%** ⇒ **假设②（中文问句 ↔ 英文段落的语义错位）成立且量级很大**。
  → **两条纪律**：① HyDE 的 10/10 是**上界不是成绩**（等于开卷考试），只能用来定位瓶颈；② 两通道高度互补（`v∪b = 7/10` 远高于任一单通道 40%）。
- **O1-R5 混合检索 RRF —— 负结果**：融合反而丢一题（4/10→3/10）→ 三个机制：① BM25 对中文查询近零信号却被**等权融合**；② `1/(60+rank)` **把名次差压平**；③ RRF 只看名次、不看"这一路可不可信"。
- **F1 论文真实 QA：90 条**（HR 意见③）：`build_paper_qa.py` 六查（条数 / 字段 / 空值重复 / **source 可追溯（quote 逐字回原文）** / **数据泄漏**（与评测题比对，阈值 0.7） / 五类分布）→ **逐字回查 90/90 通过、泄漏 0、硬错误 0**；五类：术语定义 18 / 方法步骤 22 / 实验设置 17 / 结论 25 / 局限 8。
- **Web UI 完成版 `app.py`**（Gradio）：上传 PDF → 内存建库 → 提问 → 答案 + [资料§N] 出处 + 召回片段可视化 → 顶部**从 `实验日志.md` 现读的指标对比表**（不硬编码）→ Top-K 可调 → 双模型切换 + 服务身份检查。**关键设计：界面进程绝不加载模型**（全程 HTTP 调 8000 端口）——6G 显存一次只能跑一个 3B。
- **⚠ 证据链自查：查出并修正自己报告里一处 F1 数据错误** ⭐⭐：核对时发现 R1R2 的 F1 写作 0.889，**但 0.889 是"精确率 P"，真值是 0.842**（根因：抄错混淆矩阵一个格子，`FP=0` 而非 `FP=1`，谬误自洽地"算"出来了）→ 修正 4 个文档 + 独立成一笔 commit。
  **教训**：**"多跑投票"防的是随机误差，"照抄混淆矩阵"防的是人为笔误——尺子不会算错，错的是把数字搬进文档的那一步。**

#### H4. 查询改写 / 模板消融 / 引文兜底 / 真实数据（Day19）
- **`rewrite_queries.py` 查询改写三档（term / hyde / both）—— 负结果**：总 55% → **45% / 50% / 55%（无一档超过基线）**；F1 0.824 → 0.706 / 0.571 / 0.625。
  - **但检索侧确实涨了**（strict 4→4/5/6、loose 5→6/8/7）→ **"检索涨、生成崩"是结构性的**：`hyde` 拿"假设答案片段"当查询，检索到的段落**更像答案本身** → 资料外题变得"有话说"，防幻觉 7/10→4/10。
- **改写产物质检"四看"** ⭐⭐：人工复核 20 题 + **回 `chunks.json` 计数**，把"像是幻觉"变成"**计数为 0 的幻觉**" → 四看全部不合格；**四类漏损机制**：① 改写器**编造实体**（`Generalized Mean Regression` 库内 0 次）；② **"真实存在但语义错位"**（库里 `4096` 指"仿真跑了 4096 次"、不是参数量；`GMRES` 是数值线性代数算法名）——**"像真词"的伪实体比乱码更坏**；③ **多一个连字符就丢 BM25 信号**（`LAFAN1`→`LAFAN-1`）；④ **指令泄漏**（`可能出现的表述：` 混进 7 题输出）。
- **改写后"融合"翻正** ⭐⭐ 今天最漂亮的机制证据：hybrid **3/10 → 6/10**（首次超过最好的单通道），**但上界一点没涨**（并集恒为 7/10）→ **改写做的是"命中集合重排"、不是净增益**；融合的价值在**兑现率 3/7 = 43% → 6/7 = 86%** → **"错的不是融合，是'在什么查询下融合'"**。
- **模板 09 v2 + 逐句消融（v2 / v2a / v2b）—— 负结果 + 归因锁死**：三档全线回退（F1 0.824 → 0.462 / 0.571 / 0.706；总 55% → 40% / 35% / 50%）；
  **祸首锁死在一句提示语**："只有当资料确实找不到相关信息时才拒答；**资料里有依据就照实回答**"——**带这句的两档 out 只有 3~4/10，不带的有 6~7/10**。机制：**资料外题的 Top-K 永远有 8 条**（检索总会返回东西），这句话等于告诉模型"有东西就是有依据"。
  **第二层结论（更重要）**：`v2b`（6/10）仍低于 `v1`（7/10）→ 正确说法是"**这一版提示词对生成层没有净收益**"。
- **Q11 / Q14 / Q18 答案原文复核** ⭐⭐（认知上最重要的一步）：逐句回 `chunks.json` 核对"是编的还是抄的" → **三条里没有一条是凭空编造**：Q11 是把"伪影 / 指标定义 / 用户研究结论"统一贴上「缺点」标签；Q14 是 chunk 76 原文照抄、错在**答非所问**；Q18 是把**参考文献页码**（`pp. 425–432`）当成"被引次数"。
  → **"误读型幻觉"提示词治不了**（模型"看过"这些字、不认为自己在编）——**这本身是结论**。
- **引文强制白名单（O2-G3）** ⭐⭐：`strip`（剪切非法编号）→ **交付口径非法引用 5 → 0 处**、剪除 13 处、**F1 逐格不变（0.824）**、判定与拒答**零翻转**（**唯一零副作用的工程兜底**）；`retry`（点名重答）→ F1 0.824→**0.737**（FP 0→2）**负结果不采用**（与 v2 规则②同型：**给 3B 任何"允许拒答"的措辞，它就把出口当答案**）。
  **并暴露两个读数坑**：**坑A** F1 的 FP 按 `refused` 算、只比 `correct` 会漏报；**坑B** `非法 0 处` ≠ `答案干净`（交付答案里仍有 **23 处非数字伪引文**如 `[资料©]`，正则只认数字）。
- **F2 真实行业数据 111 条** ⭐⭐（HR 意见③）：过 `--min 100`、**重复 0 / 泄漏 0 / 警告 0**、**26 个机构、最大机构占比 10.8%**；
  **分两批做（方法论重点）**：第 1 批 80 条（市场类 40% 超标、公司经营仅 8.8%）→ 第 2 批 **+31 条只补短板**（公司经营 +17、产品技术 +9、政策 +5）→ 最大类目压到 28.8%。
  ⭐ **最值钱的一条数据诚实范例**：三花智控**年报（"配合客户、构筑护城河"，乐观叙事）与 GDR 监管问询回复（"尚未有相关定点客户，亦尚未形成量产订单或收入"，监管口径）相反——两条都收进来了**。一份真实数据集应同时保留"公司叙事"与"监管口径"，只留好听的那条就是自我美化。
  **两个数据采集坑**：① PDF 会把表格抽成没有分隔符的一长串 → 数值题改引正文句；② 港交所公告是繁体 + 排版空格 → **只去排版空格、保留繁体原字、不转简体**（转简体会让"逐字回查"失效）。
- **F3 真实 vs 合成对照微调 —— 负结果，如实记录**：训练本身成功（`train_loss=2.199` / `eval_loss=2.388` / 显存峰值 **2.11GB** / 可训练 **0.4827%**）；**检索两列一字不差（自检通过）**；但防幻觉 **7/10 → 2/10**、F1 0.824 → **0.333**、总 55% → **35%**。
  **⚠ 归因边界**：本轮「**真实 201 条**」vs「**合成 300 条**」——**"来源"与"数量"两个变量同时变了**，不能归因给"数据不真实" → 主动补一轮拆变量（见 H5）。

#### H5. 成果定稿、双模型对比与在线交付（Day20）
- **`collect_finals.py` + `定稿数字表.md`（唯一数字源）** ⭐⭐：从各结果目录 `run_log.txt` **机器抄录**（不手抄）+ 三项自动校验（**F1↔混淆矩阵自洽** / 一致率 ≥0.93 / 轮次目录存在）→ **6 行全绿、问题清单为空**。
- **《优化成果报告》定稿** ⭐⭐：**表一 17 轮**（从 day14 原始基线到 F4l）+ **表二逐步骤贡献 12 行** + **失败尝试 8 条** + 真实数据整改 + 拆变量轮 + 双模型对比；**全文无「待跑」/占位符**。
- **`优化前后对比.png`**：双面板柱状图（左四指标 + 右 F1），**三条口径写在脚本里，不许混着减**。
- **F3v3 拆变量轮（真实 201 + 合成 99 = 300）** ⭐⭐：`build_mix_data.py`（三条硬检查 + `--dry-run` 先看账）→ 训练（与 v2 **只差 3 个常量**）→ **F1 0.429**、防幻觉 3/10、总 40%；**检索两列与 R3 逐位相同**。
  → **结论：数量对齐到 300 后只回了 1 题（2/10→3/10），距 R3 的 7/10 仍差 4 题 ⇒ 退化主因是"数据分布与评测集不匹配"，不是数量**（v2 数据里 111/201 = 55% 是行业语料，而评测题出自 GMR 论文 → 领域适配被摊薄）。
- **F4 双模型对比（原模型 vs 微调）** ⭐⭐ 本周最重要的一轮，**逆预期负结果**：

| 指标 | 微调模型（R3） | **原模型（F4o）** | 差 |
|---|---|---|---|
| 检索 strict / loose | 4/10 ｜ 5/10 | **4/10 ｜ 5/10（逐位相同）** | 0 |
| 生成（资料内） | 4/10 | **7/10** | +3 |
| 防幻觉（资料外） | 7/10 | **9/10** | +2 |
| **防幻觉 F1** | 0.824 | **0.947** | **+0.123** |
| 总正确率 | 55% | **80%** | +25 个百分点 |

  → **原模型在两个生成类指标上全面占优 ⇒ 这版数据/配置的 LoRA 在防幻觉上是负收益**（**反例成了主结论，照实记录**）。
  **边界三句同时写**：① 只能说"这版微调在防幻觉上负收益"，**不能**推广成"微调无用"；② 微调收益在**风格/术语/格式遵循**（可展示，但不是本评测集判据）；③ n=20、CI≈±0.22，但 **−0.123 的 F1 差 + 全项同向**足以支撑量级判断。
  **正面收获**：原模型在检索层完全一样的情况下拿到 80% → **知识确实来自检索层**，这正是"**为什么选 RAG 而不是直接微调**"的实测回答。
- **`check_release.py` 发布闸门**：六项 + 新增"检索口径与建库自举"6 条（体积 / 禁入项 / 密钥 / 依赖 / 标注纪律 / 占位符与数字对账）→ **实测全绿，文章占位符归零**。
- **B 线在线 Demo 上线** ⭐⭐：`modelscope.cn/studios/LanMercer/rag233`（直达 `lanmercer-rag233.ms.show`），代码仓库 `github.com/LanMercer/rag233`（Public）。
  **方案 A：内置示例语料** ⭐⭐ 最有说服力的工程细节：把 GMR 论文 105 段做成内置语料 → **不传 PDF 也能问**（首问 31s → **2~3s**）；**实测线上出处编号与离线评测落盘 `top_ids` 20/20 一致** → **访客能自己核对线上结果**。
  过程中踩出**四个"不报错也查不出来"的坑**：collection 追加（105→210）/ `hnsw:search_ef` 默认只有 10 / `if progress:` 越界 / 内存库线程生命周期。
- **根 `README.md`**（仓库门面）：成果表 / 在线 Demo（含"不传文件也能试" + 诚实标注）/ **7 条负结果** / 快速开始 / 仓库结构 / 四个隐性坑 / 诚实性声明。
- **P 线叙事与简历**：端到端复述脚本（10 分钟，含演示 3 问）+ **8 条"别人抄不走的"个性化点** + `面试追问自测表.md`（**30 问**，含 HR 新增 3 问）；简历项目段改写为**结果句**。

#### H6. 第四周的六条方法论收获（面试可直接讲）
1. **评测数字 = 统计估计**：n=20 时 CI≈±0.22，看量级不看小数点；单次生成有随机性 → **对外引用的定稿数字必须多跑取多数 + 报一致率**。
2. **优化 = 固定评测集上的受控实验**：**每轮只改 1 个变量**，单独重跑 20 题，写入 `实验日志.md`；说不清是谁起的作用就等于没做实验。
3. **先修尺子的偏差、再压方差，然后才敢说提升多少**（偏差-方差权衡的工程版）：判据过严会误杀正确答案、口径不统一会让前后不可比。
4. **上界 ≠ 成绩**：人工手写理想查询的 10/10 只能定位瓶颈，真实现（模型自动改写）落在 50% —— 把两者混为一谈就是自欺。
5. **失败尝试也是成果**：**8 条失败记录 + 2 轮逆预期主结论**，每条都有机制解释——`实验日志.md` 本身就是"我怎么做优化"的证据。
6. **数字只能有一条通道**：`run_log.txt` →（机器抄录 + F1↔矩阵自动对账）→ `定稿数字表.md` → 所有对外文字；发布前用脚本拦占位符。
  **最重要的一句**：**"我报的是跑出来的数，不是想要的数"**——把两轮难看的结果留在汇报里，比多涨几个点更能证明实验可信。

---

### 模块 I｜副线：力扣刷题 + 统计建模（Day1~20 零散时间）

#### I1. 力扣刷题（每天 1~2 题，全部零散时间，按分类提交 GitHub `leetcode` 仓库）
| 专题 | 对应 Day | 完成题目 | 数学加成点 |
|---|---|---|---|
| 数组 + 哈希表 | Day1~5 | 1、217、26、27、88、121、242、383、283、169 | 哈希 = 空间换时间 |
| 字符串 + 链表 | Day6~10 | 344、125、14、415、206、21、876、141、160、382 | **382 蓄水池抽样**（`P(第k个)=1/n` 裂项相消）、**160 相交链表**（路程差追及） |
| 排序 + 递归 + 二分 | Day11~15 | 912、215、56、75、104、226、704、35、34、69 | **215 快速选择**（期望 O(n) 等比级数求和）、**69 牛顿迭代**（二次收敛） |
| 动态规划 | Day16~20 | 509、70、198、53（152 备好） | DP = 用递推避免重复计算、空间换时间 |
- **递归 = 数学归纳法**（归纳基 + 归纳步）：`depth(node) = 1 + max(depth(left), depth(right))`。
- 牛客"银行/央企"笔试真题 1 套（混合题型：选择 + 编程 + 行测）。

#### I2. 统计建模副线（每天 10~15 分钟，覆盖银行风控岗）
| 主题 | 对应 Day | 能讲什么 |
|---|---|---|
| 逻辑回归 | Day6 | sigmoid、决策边界、与线性回归区别 |
| 评估指标 | Day7 | 准确率 / 精确率 / 召回率 + 混淆矩阵四格 |
| F1 / AUC | Day8 | F1 调和平均惩罚不均衡；**AUC 双视角等价证明**（概率口径与几何口径同算 0.833） |
| 偏差-方差 | Day9 | `MSE = 偏差² + 方差 + 噪声σ²` 完整推导 |
| 决策树 / 随机森林 / GBDT | Day10 | 信息增益 / 基尼系数直觉；bagging vs boosting |
| 评估指标复习 | Day11 | 混淆矩阵 + P/R/F1 + AUC 概率口径（衔接 RAG 评测） |
| 交叉验证 | Day12 | K 折 / 留一法 / 偏差-方差权衡（衔接 90/10 验证集） |
| 特征工程 | Day13 | 缺失值机制 MCAR/MAR/MNAR、one-hot vs 标签编码、标准化 vs 归一化"**看算法不看数据**" |
| 样本不均衡 | Day14 | SMOTE 特征空间插值 vs 复制、类权重 = 软版过采样 `w=n/(k·n_j)` |
| 过拟合处理 | Day16 | L1/L2 正则化、早停、Dropout，各自对应到本项目 |
| 6 节串线 ①②③ | Day18~20 | 把"逻辑回归 → 评估指标 → 交叉验证 → 特征工程 → 样本不均衡 → 过拟合"串成**建模全流程一条线**；**RAG 评测 vs 统计评估对照**（Recall@K ↔ 召回率、防幻觉 F1 ↔ 分类 F1、n=20 的置信区间） |

---

## 五、项目成果（简历项目段落可直接引用）

### 5.1 项目描述（结果句版本，数字均来自 `定稿数字表.md`）

```markdown
### 机器人领域 RAG 文档问答系统（个人项目 · 可在线体验）

**在线 Demo**：modelscope.cn/studios/LanMercer/rag233 ｜ **代码**：github.com/LanMercer/rag233
**技术栈**：Python / PyTorch / Transformers / PEFT / bitsandbytes / LangChain / Chroma / FastAPI / Gradio / Qwen2.5-3B-Instruct / bge-small-zh-v1.5

- **交付一个开箱即用的领域问答工具**（Web UI + 一键启动脚本 + 内置示例语料）：上传 PDF 自动建库、提问返回带 [资料§N] 出处的答案，支持"原模型 / QLoRA 微调模型"一键切换；已部署公网在线 Demo，**不传文件也能直接提问**。
- **针对检索命中率 20% 的基线瓶颈做了一组可归因的优化实验**（TOP_K 4→8、bge query instruction、查询改写 HyDE、BM25+RRF 混合检索、模板 09 消融、引文白名单兜底），在**同一 20 题固定评测集**上（k=8、多跑取多数、双轨判据）把
  **检索命中率 20% → 40%、生成正确率 20% → 40%、防幻觉 F1 0.667 → 0.824、总正确率 40% → 55%**（引文合法性兜底后可核验率 100%，总正确率 60%）；
  **17 轮对比 + 12 行逐步骤贡献归因 + 8 条失败尝试记录**，每一步"我做了哪一步 → 哪个数字从 X 变成 Y"可复现。
- **用真实数据重构 SFT 集**：从论文人工提取真实 QA **90 条**（逐字回查 90/90、泄漏 0）+ 采集公开机器人行业资料构造 QA **111 条**（26 个机构、每条标注来源可追溯、最大来源占比 10.8%）；QLoRA（4bit 基座 + LoRA r=8）**仅训练 0.4827% 参数**，显存峰值 2.11GB；
  并做了"真实 vs 合成"拆变量对照实验，**如实记录为负结果并归因到数据分布不匹配（非数量）**。
- **分层评测体系（检索 / 生成 / 防幻觉三组 + 混淆矩阵 F1）+ 人工复核诚实改判**，能把每个低分归因到具体环节；含**原模型 vs 微调模型同评测集对比**（原模型 F1 0.947 / 80% vs 微调 0.824 / 55%）——反例照实记录，并据此证明**知识来自检索层**（RAG 的价值实证）。
```

### 5.2 四层架构（面试讲架构用）

| 层 | 组件 | 负责什么 |
|---|---|---|
| 检索层 | PyPDF 加载 → 中文切块（500/50）→ bge-small-zh 向量化 → Chroma 余弦 Top-K | 找出相关段落（"答对"前提） |
| 答案组织层 | 模板库 v2 · 模板 09（资料带编号 [N] + 强制 [资料§N] 出处 + 无内容就直说） | 拼"只依据资料"的提示词 |
| 风格/能力层 | QLoRA LoRA adapter（`W = W0 + B·A`，只训 0.48% 参数） | 让回答像行业专家 |
| 生成层 | Qwen2.5-3B-Instruct 4bit → OpenAI 兼容 `/v1/chat/completions` | 把提示词变成通顺答案 |

- **数据流**：问题 → 检索层向量化 + Top-K → 模板 09 拼上下文 → Qwen（挂 adapter）生成 → 带 `[资料§N]` 出处的答案。
- **数学视角**：`P(答案|问题) ≈ P_生成(答案 | 模板09(检索(问题)))` —— **分层 = 条件概率分解**，每层独立调优、独立评估。
- **面试加分句**："**RAG 管事实（答对）、微调管风格（答得像）**，两条路线互补；我的系统是四层解耦的，每层可以独立调优、独立评估——这正是我的评测能定位到具体环节的原因。"

### 5.3 优化前后指标（定稿口径：同 20 题固定评测集、k=8、T=0.2、runs=3 多数投票、双轨判据）

| 指标 | 优化前（Day14 基线） | **优化后（定稿 R3）** | 变化 | 带引文兜底（R6） |
|---|---|---|---|---|
| 检索命中率（strict） | 20% | **40%** | **+20 个百分点** | 40% |
| 生成正确率（资料内） | 20% | **40%** | **+20 个百分点** | **50%** |
| 防幻觉正确率（资料外） | 60% | **70%** | +10 个百分点 | 70% |
| 总正确率 | 40% | **55%** | +15 个百分点 | **60%** |
| 防幻觉 F1（TP/FP/FN/TN） | 0.667（6/2/4/8） | **0.824**（7/**0**/3/10） | +0.157，**FP 归零** | 0.824（逐格不变） |
| 引文非法处数（交付口径） | 未采集 | — | — | **5 → 0**（零副作用兜底） |

> **口径纪律（不许混着减）**：优化前是 `runs=1` + 旧判据（仅 strict）；定稿是 `runs=3` + 双轨判据 + 判据校准。**同口径对照**应看 `B0r3 → R3`（检索 **20% → 40%**、总 **40% → 55%**）。

### 5.4 三条最有价值的归因结论（面试最看这张）
- ⭐ **唯一稳定有效的一步是 TOP_K 4→8**（机制：期望段从"第 5 名"回到窗口内）；
- ⭐ **唯一零副作用的工程兜底是"引文强制白名单"**（交付口径非法引用 5→0 处、F1 逐格不变）；
- **三类尝试无净增益**（均已给出机制）：bge instruction 前缀（排名位移）、查询改写三档（伤专名 / 伤拒答）、模板 v2 系（归因锁死一句提示语）、`retry` 兜底（出口条款被滥用）。

---

## 六、量化成果速查表（四周全量，数字会说话）

### 6.1 学习链（Day1~12）

| 指标 | 数值 | 对应 Day |
|---|---|---|
| 手写多头注意力 vs 官方实现误差 | **5e-07** | Day4 |
| 第一个 seq2seq 模型 | loss 1.856→0.000，测试 **10/10** | Day4 |
| 注意力权重矩阵（3×3） | 每行和为 1 = 转移概率矩阵 | Day3 |
| 词向量改造实验（猫-狗互相关注权重） | 0.212 → **0.468** | Day3 |
| LoRA 参数节省 | **99.55%**（全量 d² → 2dr） | Day5 |
| LoRA 低秩近似误差（512×512, r=16） | **1.63e-15** | Day5 |
| 灾难性遗忘实证（任务 A 误差） | 0 → 3.93 | Day5 |
| Qwen2.5-3B 4bit 显存（预算/实测） | 3.0GB → **1.92GB** | Day5/Day6 |
| 思维链 A/B（数学正确率） | 60%→**84%**（N=5）；20%→**50%**（N=20） | Day7/Day8 |
| 提示词模板库 | v1（10）→ **v2（20 个 / 5 大分类）** | Day7/Day8 |
| 模板迭代闭环 | 模板 07 v1→v3 **四字段全对** | Day8 |
| RAG 问答 | 自备论文 **3 问全对**，带出处；防幻觉通过 | Day10 |
| 手写 RAG vs LangChain | 结果基本一致（原理实证） | Day10 |
| SFT 数据 | **300 条** / 三字段 / 6 类任务 / **0 重复** | Day11 |
| QLoRA 训练参数量 | **0.4827%**（14,966,784 / 3.10B） | Day12 |
| 微调产物体积 | adapter 仅 **20~30MB** | Day12 |
| 微调前后对比 | 5 题中 **4 题**肉眼可见差异 | Day12 |

### 6.2 项目链（Day13~20）

| 指标 | 数值 | 对应 Day |
|---|---|---|
| 评测集设计 | 20 题（资料内 10 + 资料外 10，可自动判分） | Day13 |
| 四层架构 + 架构图 | 1 张（检索/组织/风格/生成） | Day13 |
| 首版基线 | 检索 20% / 生成 20% / 防幻觉 60% / 总 40% / F1 **0.667** | Day14 |
| 一键切换 | `config.json` + `ask.py` 双 profile + 服务身份检查 | Day15 |
| 评测尺子 | `eval_v2.py` 737 行 / **15 个参数** / 双轨判据 | Day16 |
| 实验记录 | **17 轮**对比 + **12 行**逐步骤贡献 + **8 条**失败尝试 | Day16~20 |
| 数据自检 | **9/10 标注自洽、0 错位** | Day18 |
| 检索诊断 | plain：vector 4/10、bm25 4/10、并集 **7/10**；HyDE 上界 10/10 | Day18 |
| 融合兑现率（改写后） | **43% → 86%** | Day19 |
| 引文兜底 | 交付口径非法引用 **5 → 0 处**，F1 逐格不变 | Day19 |
| 论文真实 QA | **90 条**（逐字回查 90/90、泄漏 0） | Day18 |
| 行业真实 QA | **111 条**（26 机构、最大占比 10.8%、泄漏 0） | Day19 |
| 真实数据合计 | **201 条可追溯** | Day18~19 |
| 训练账（v2/v3） | 显存峰值 **2.11GB**；`671.4s` / `473.7s`；可训练 **0.4827%** | Day19~20 |
| **优化后定稿（R3）** | 检索 **40%** / 生成 **40%** / 防幻觉 **70%** / 总 **55%** / **F1 0.824**（FP=0） | Day20 |
| 带兜底（R6） | 总 **60%** / 引文非法 0 处 | Day20 |
| 双模型对比（F4o） | 原模型 **F1 0.947 / 80%** vs 微调 0.824 / 55%（**逆预期负结果**） | Day20 |
| 在线交付 | 公网 Demo（**不传 PDF 也能问**、首问 31s→**2~3s**）+ Public 仓库 | Day20 |
| 线上/离线一致性 | 出处编号 **20/20 一致**（访客可自行核对） | Day20 |

### 6.3 力扣与统计副线

| 指标 | 数值 | 对应 Day |
|---|---|---|
| 力扣累计 | **40+ 题**，覆盖 9 个专题（含概率/数学加成题 382、160、215、69） | Day1~20 |
| 统计建模 | **6 大主题 + 6 节串线**，全部能与项目实操接上线 | Day6~20 |
| 牛客"银行/央企"真题 | 1 套限时完成 + 复盘 | Day10/Day19~20 |

---

## 七、工程能力：踩坑与解决素材库（面试"踩坑-解决"精选）

| 类别 | 问题 | 解决办法 | Day |
|---|---|---|---|
| 环境 | transformers 新版要求 torch≥2.6 与 cu121 驱动冲突 | 换用 safetensors 格式迷你模型绕开 | Day1 |
| 数值 | 手写多头注意力 vs 官方误差 1.87 | PyTorch 线性层是 `y = x·Wᵀ`，转置修复 → 5e-07 | Day4 |
| 部署 | `git add .` 误把 6GB 模型权重加入暂存区 | `.gitignore` 排除 + `git rm --cached`；**模型权重永远不进 git** | Day6 |
| 系统 | PowerShell 中文乱码 / GBK 编码错误 | `chcp 65001` + `$env:PYTHONIOENCODING='utf-8'`；脚本内置 UTF-8 加固 | Day2 起 |
| 依赖 | langchain 1.x 切块模块报 ModuleNotFoundError | 改用独立包 `langchain-text-splitters` | Day10 |
| 数据 | 生成 SFT 数据检出 27 条完全重复 | 扩大语料池 + 生成循环去重 `if key in seen: continue` → 0 重复 | Day11 |
| 版本 | `Trainer` 报 `unexpected keyword argument 'tokenizer'` | 用 `inspect.signature` 查新签名，改名 `processing_class`（另两处 `warmup_steps` / `dtype`） | Day12 |
| 内存 | 启动内存 `os error 1455`（fp16 shard 整块进内存量化超 RAM） | `low_cpu_mem_usage=True` + `dtype`；修复后加载 434/434 仅 6 秒 | Day14 |
| 生成 | Q5 整段无限复读模板指令刷满 512 token（退化吸收态） | `repetition_penalty=1.15` + `no_repeat_ngram_size=4` 双保险；**低温要配防复读参数** | Day14 |
| 评测 | 规则判分漏网（Q9 关键词碰巧命中、Q18 末尾补拒答词） | `MANUAL_FIXES` 人工复核改判，**宁可数字变低** | Day14 |
| 评测 | 无改动下也有运行间波动（生成 1/10 vs 2/10） | **定稿数字一律多跑取多数 + 报一致率（≥0.93）**；日常实验仍 runs=1 省时 | Day16 |
| 评测 | 判分关键词过严误杀正确答案（`LAFAN1` vs `LAFAN`） | `KEYWORD_PATCH` 补丁表（覆盖而非改原 json + 开关 + 只放宽到前缀）；**必须全量重跑前一次性统一** | Day17 |
| 评测 | **抄错一格混淆矩阵 → F1 整列失真**（0.889 是精确率 P，真值 0.842） | 靠"**文档数字 ↔ `run_log.txt` 原始输出**逐行对账"抓出 → 修正 4 个文档 + 独立 commit | Day18 |
| 实验 | **官方推荐做法反而变差**（bge instruction 前缀：检索 20%→0%） | 逐题追查 → 机制是"**排名位移**"而非语义变差 → 记录负结果并回退 | Day17 |
| 实验 | **判据假阳性**（Q7 答案中间带拒答词被记成"该答没答"） | 报告明说这个 FP 不是真实拒答；引出"轮内双口径"设计 | Day19 |
| 实验 | 一次改两个变量说不清谁起作用（F3 的"来源"和"数量"同时变） | **主动补一轮拆变量**（真实 201 + 合成 99 = 300）→ 才敢说"主因是分布不匹配" | Day20 |
| 环境 | `pip install -r requirements.txt` 把共享 `llm` 环境打坏（41 个包降级） | 判定"**环境错不是代码错**"→ 按卸载日志逐包装回；固化成独立 `demo` 环境；纪律：**跑前先问"这份清单是给谁的环境用的"** | Day18 |
| 环境 | gradio 4.44 只写 `pydantic>=2.0` 无上界 → pydantic≥2.11 让服务"能起、每个请求 500" | **单变量对照实验**：2.12.0 复现 → 2.10.6 通过 → pin 是**实证过的修复** | Day18 |
| 环境 | `.condarc` 写了中文注释 → conda 全线报 `UnicodeDecodeError: 'gbk'` | **配置文件必须纯 ASCII**；教训：给配置文件写注释前先确认解析它的人用什么编码 | Day18 |
| 上线 | 创空间状态 `Running` 但接口全 502 | 裸 `launch()` 默认绑 `127.0.0.1` → `launch(server_name="0.0.0.0", server_port=7860)` | Day20 |
| 上线 | 出处重复（同一段出现两次，有效命中 4→2） | `Chroma.from_documents` 不传 `collection_name` → 同进程**追加**（实测 105→210）→ 每次建库给唯一 `collection_name` | Day20 |
| 上线 | **线上出处编号与离线评测对不上（13/20），且每次建库结果都不同** ⭐ 最阴的一个 | `chromadb` 的 `hnsw:search_ef` **默认只有 10**（比库容量 105 还小）+ 同篇论文余弦分挤在 0.8~1.0 窄带 → 边界必抖 → 显式设 `hnsw:search_ef: 200` → **20/20 一致** | Day20 |
| git | `.gitignore` 加了规则 `check-ignore` 仍无输出 | **`.gitignore` 只管"未跟踪文件"**，已进过提交的文件加规则也拦不住且 `git status` 不显示 → 四步修 + `git rev-list --objects` 验收 | Day19 |
| git | `git push` 三层坑串联（代理未显式给 git / LFS 锁校验 / schannel TLS 后端） | 逐层修：`-c http.proxy=...` → 关 `locksverify` → `http.sslBackend=openssl` + HTTP/1.1 | Day18~19 |
| 文档 | 模板里的 `--date 9/23` 占位符被照抄 4 次 | 占位改成 `--date 填你的执行日` + 警告；**模板里的示例值一定会有人照抄，所以要让它抄不出错** | Day19 |

---

## 八、简历技能栈速查

- **框架与库**：PyTorch 2.5（GPU）、HuggingFace Transformers / PEFT / Datasets / Accelerate、bitsandbytes（4bit NF4 量化）、LangChain、FastAPI、Chroma、Gradio、Jupyter。
- **模型**：Qwen2.5-3B-Instruct（本地 4bit 部署 + QLoRA 微调）、bge-small-zh-v1.5（中文 Embedding）、DeepSeek API（在线 Demo 生成层，已诚实标注）。
- **技术点**：Transformer 原理、注意力机制、位置编码、残差/LayerNorm/FFN；量化部署（NF4）、采样参数（temperature / top_p）；提示词工程（few-shot / CoT / 结构化模板 / 防幻觉约束）；**RAG 全链路**（加载→切块→Embedding→向量库→Top-K→生成）；**检索优化**（TOP_K 调优、query instruction、HyDE 查询改写、BM25 + RRF 混合检索、引文白名单兜底）；**LoRA/QLoRA/PEFT 微调**；OpenAI 兼容接口服务化；**评测体系设计**（分层指标 + 混淆矩阵 F1 + 多跑投票 + 人工复核）。
- **工程**：Git（含 `.gitignore` 陷阱、历史清理、代理/LFS/TLS 排查）、Windows 环境问题排查、依赖版本兼容（pin 实证）、Gradio 部署上线、发布前自动校验闸门。
- **数学/统计**：概率分布与条件分布视角、低秩近似（SVD）、方差分解、贝叶斯先验/后验、熵、余弦相似度/最近邻、**Recall@K**、精确率/召回率/F1/AUC、混淆矩阵、交叉验证、偏差-方差权衡、A/B 实验设计、置信区间。
- **其他**：LeetCode 40+ 题（数组/哈希/双指针/字符串/链表/排序/递归/二分/DP）。

---

## 九、每日学习轨迹一页总览（Day1~Day20）

| Day | 周 | 主题 | 一句话成果 |
|---|---|---|---|
| Day1 | 一 | 环境搭建 | GPU 版 PyTorch 跑通，`torch.cuda.is_available()`=True |
| Day2 | 一 | Transformer 图解 | 整体结构图草稿 + 概率语言概念笔记 |
| Day3 | 一 | 注意力机制可视化 | 手算 3×3 注意力权重矩阵 + √d 方差解释 + 词向量实验 |
| Day4 | 一 | 多头注意力与位置编码 | 手写 vs 官方误差 5e-07；seq2seq 训练 10/10 |
| Day5 | 一 | 结构图定稿 + 微调认知 | 定稿结构图（残差/LN/FFN）+ LoRA 数学 demo + 显存方案 |
| Day6 | 二 | Qwen 本地部署 | 4bit 部署跑通，显存实测 1.92GB，temperature/top_p 对比 |
| Day7 | 二 | Prompt 工程入门 | few-shot/CoT/结构化模板 + 思维链 A/B（60%→84%） |
| Day8 | 二 | Prompt 进阶 + 模板库 v2 | 20 模板 5 分类 + 模板 07 迭代闭环 + 模板 09 RAG 版 |
| Day9 | 二 | 模型接口化 | FastAPI OpenAI 兼容接口，curl/requests/LangChain 三路调通 |
| Day10 | 二 | RAG 全链路 | 上传 PDF 能问答 demo，GMR 论文 3 问全对 + 防幻觉通过 |
| Day11 | 三 | 微调（一）数据与环境 | 300 条机器人行业 SFT 数据（0 重复）+ 环境五件套全绿 |
| Day12 | 三 | 微调（二）跑通训练 | QLoRA 只训 0.48% 参数，产出 adapter，对比表 4/5 有效 |
| Day13 | 三 | 项目定性 + 架构定型 | 四层架构图 + 20 条评测题 + 微调模型接入接口（curl 三通） |
| Day14 | 三 | 量化评测 | `eval.py` 出首版数字：检索 20% / 生成 20% / 防幻觉 60% / F1 0.667 |
| Day15 | 三 | MVP 整合 + 完结 | 一键切换 + README + 效果评估报告 + **完结验收 5 条全 ✅** |
| Day16 | 四 | 尺子校准 + Web UI 骨架 | `eval_v2.py`（15 参数 + 双轨判据 + 投票）+ B0 基线 + R2（首个优化数字） |
| Day17 | 四 | 首轮受控实验 | bge instruction 前缀负结果（20%→0%）+ 机制追查 + 决策门 + 一键启动 |
| Day18 | 四 | 瓶颈定位 + 论文真实数据 | 数据自检 9/10 自洽；**HyDE 上界 10/10 证明"查询侧语言错位"**；90 条论文 QA；修正自己一处 F1 笔误 |
| Day19 | 四 | 改写/模板/兜底/真实行业数据 | 改写三档负结果 + 融合兑现率 43%→86% + 模板 v2 归因锁死 + 引文兜底 5→0 + **111 条行业真实数据** |
| Day20 | 四 | 成果定稿 + 双模型对比 + 上线 | 定稿数字表（6 行全绿）+ 优化成果报告（17 轮/12 行/8 条）+ F3v3 拆变量 + **F4o 逆预期** + **在线 Demo 上线** |

---

## 十、当前状态与下一步

### 10.1 已完成（四周全部主线）
- **能力链闭环**：原理（Day1~5）→ 部署（Day6）→ 提示词（Day7~8）→ 接口化（Day9）→ RAG（Day10）→ 微调（Day11~12）→ 项目定型与评测（Day13~15）→ 优化工程与交付（Day16~20）。
- **项目交付物**：公网可访问的在线 Demo + Public 代码仓库 + 根 README（含 7 条负结果）+ 优化成果报告（17 轮对比 / 12 行贡献 / 8 条失败）+ 项目架构图 + 优化前后对比图 + 201 条可追溯真实数据。
- **面试弹药**：端到端复述脚本（10 分钟）+ 8 条个性化点 + 30 问面试追问自测表 + 简历结果句。

### 10.2 如实标注的遗留（均有去向）
1. **在线 Demo 浏览器实走一遍**（不传 PDF 直接问 + 传 PDF 覆盖）—— 待本人手动验收。
2. **Demo 可见性与 API Key 私密配置** + 本地"真 key 冒烟" —— 待本人确认。
3. **复述彩排 3 次 + 自测体检**（需本人出声掐表后填写记录表）。
4. **力扣 152 完成状态待确认**；**322 零钱兑换**未做（移至第五周）。
5. **评测集 n=20（CI≈±0.22）是当前最大软肋**。

### 10.3 下一步（第五周起，从以下挑两条主攻）
1. ⭐ **评测集扩容到 n≥60**——把所有结论的置信度从 ±0.22 收到 ±0.13；
2. **Rerank 的真实现**（评测侧已验证"改写后做融合"能翻正，但**尚未接回生产链路**）；
3. 备选：混合检索 BM25 + RRF 落地（改写后已测出正贡献）；RAGAS / LLM-as-judge 自动化评测接入；把"误读型幻觉"交给更大模型或数据（**提示词已证明治不了**）；
4. 收尾：Demo 浏览器验收 + 真 key 冒烟 + 复述彩排 + 力扣 322 补做 + 统计八股 6 节串线复习。

### 10.4 建议配套复习路径
- **按 Day 定位**：`第X周/dayY/` 文件夹 → "详细教程 + 脚本 + 工作汇报"三件套。
- **核心速查**：本文件第五节（项目成果）与第六节（量化速查表）。
- **面试前重点演练**：
  - "三句话总结"（第一节）；
  - "四张账"（数据账 / 参数账 / 显存账 / 训练账）+ **项目账**；
  - "项目四问"（为什么做 / 架构怎么选 / 效果怎么量化 / 踩过什么坑）；
  - **优化归因三句**（唯一有效的一步是 TOP_K 4→8；唯一零副作用兜底是引文白名单；三类尝试无净增益且有机制）；
  - **两条诚实性武器**（8 条失败记录 + 2 轮逆预期主结论）。

---

> **汇报依据**：`00-基础内容总纲.md`、`第一周/week1工作汇报.md`、`第二周/week2工作汇报.md` + `01-第二周详细计划.md`、`第三周/week3工作汇报.md` + `01-第三周详细计划.md` + `02-项目完结冲刺计划.md`、`第四周/week4工作汇报.md` + `01-第四周详细计划.md` + `01-第四周项目总结与优化路线图.md` + `实验日志.md` + `day20/定稿数字表.md` + `day20/优化成果报告.md`，以及各日详细教程、脚本实测运行结果与各周汇报中记录的 git 提交（如第三周 `00c3790`→`3001cc4`「项目完结」、第四周 `2629b36`→`4a895f7`「整改定稿」）整理。
> **数字纪律**：本汇报数字全部来自各周汇报与 `定稿数字表.md`（机器抄录）；**不做跨口径相减**；**未跑出来的一律不填**；**未完成项（10.2）如实标注**，绝不虚报。
